# CNWM v7 — "The Last Ten Percent": QA session → 100%

Repo (local): `<P>` = `/Users/thebayniac/Documents/(A) Documents/(A) WBM Enterprises/(B) Notable/(B) Clients/Charles Nalle/cnwm-v2` (always quote — parentheses/spaces). Repo (remote): **https://github.com/makeitnotable/Charles-Nalle-Walking-Memorial** — local `main` tracks **`origin/v2`** (the deploy branch); plain `git push` only, **never `main:main`** (`main`, `match-figma-designs`, `feat/*`, `fix/*`, `test-branch` are the legacy SPA and must not be touched; the one open PR "Match figma designs" is legacy — ignore). Live: **https://makeitnotable.github.io/Charles-Nalle-Walking-Memorial/** (GH Pages, deployed by `.github/workflows/deploy.yml` on every push to `v2`). **Verified 2026-08-15 at planning time:** remote `v2` head = local HEAD = `623b13a`, tree clean, last deploy run `success` from that SHA, and every Part-A root cause below re-checked against the LIVE HTML/CSS/JS (Arial-Black favicon, `.lines .line-box{overflow:hidden}`, "Five spots" chip + `GeolocateControl` + bottom `Stop the walk` in the map bundle, all ten museum works `aspect: 1.5`, all grid tiles `aspect-[3/2]`, single `rel=icon` link). Content is COMPLETE (copy locked by Kathy 8/9, new narration live 8/15) — this run is craft, accessibility and UX only.

Source of this plan: Wil's page-by-page review call, 2026-08-15 15:48 MDT — `<project>/Meeting 2026_08_15 15_48 MDT - Notes by Gemini.md` (Notes = lines 1–113; verbatim transcript = lines 114–571; lines 572–600 are 15 base64 screenshots `[image1..15]`, decodable with a 6-line python snippet if a picture is ever needed). Every request in that document is enumerated in Part A. Three exploration passes over the codebase produced the file:line anchors below — **they are current as of `623b13a`; re-verify with a quick grep before editing, never assume line numbers survived earlier phases.**

## Context

The site is ~90% done. Wil's verdict: **not a redesign** — the last 10% is (1) layout craft (rag, orphans/widows, clipped letterforms, spacing rhythm), (2) accessibility (contrast, focus, keyboard, ARIA), (3) subtle UX (map/walk feel, curtain smoothness, museum interaction, mobile ergonomics), plus a real favicon. Method = a **quality-assurance session**: first *identify* every issue by audit (instruments + fresh eyes) across phone / tablet / desktop, then *apply* the solution to each, then prove it with a fresh-juror gate. Nothing here changes Kathy's facts; punctuation-only edits to locked prose are logged.

## Locked decisions (Wil, 2026-08-15 review + planning Q&A)

1. **1858 map lens — reconcile Wil + Kathy:** the FULL LOC plate stays loaded and pannable (Kathy 8/9: "do not crop, allow pan and zoom"), but the **initial view AND the reset view frame only the lower panel** (downtown Troy + the Hudson + West Troy) scaled to fill a **much larger viewport**; the upper panel is reachable by panning; no quality degradation.
2. **Em dashes go everywhere** — UI, People, About, labels, addresses, titles, AND the chapter JSON prose — but inside Kathy-locked prose (`scenes[].paragraphs`, `historicalContext`, `portal.history`, `morals[].message`, `sketchNote`, quotes) **punctuation-only substitutions** (comma / colon / period / parentheses / line break), **zero word changes** so text still matches the ElevenLabs audio word-for-word; every edit logged in `docs/CONTENT-STATUS.md`; a courtesy note for Kathy in the REVIEW-GUIDE. En dashes in date ranges (`1859–1946`) stay.
3. **Favicon = Caslon "CN" monogram** — the Charles/Nalle wordmark reduced to an interlocked C+N set in Libre Caslon Display, converted to **paths** (no font dependency), on the site's dark ground; full icon set (SVG, PNG 16/32/48/180/192/512, real multi-size ICO, apple-touch-icon, web manifest) and `<head>` wiring.
4. **Run mode = fully autonomous, v6-style:** audit → fix → verify with no check-ins; commit + push continuously (live updates as it goes); fresh-juror gate ×2 at the end; `docs/v7/REVIEW-GUIDE.md` for Wil with before/afters and the human queue.
5. **Copy (Wil's words):** home CTA `Walk the story`; map chip `April 27, 1860`; walk button `Stop the walk` ⇄ `Continue`; mobile walk exit `Back`; People eyebrow `Their story lives on`; About eyebrow keeps `The streets are waiting`; card titles `Holeur's / Fashionable Bakery` and `Commissioner's / Office` on two lines, other three unchanged.
6. **Chapter 2 order:** Part 1 → Historical context → Moral 1 → Part 2 → Moral 2 → Where to next.
7. **Barbershop story body:** text → image → text → image → text (finish on text). Hero focus moved up to the faces.
8. **Motion additions are tasteful and meaningful:** parallax on the moral/theme background YES; parallax on archival images NO (keep the wipe reveal, make it smooth); scroll effects only where they narrate something.
9. **Museum:** camera pitched down a little (more floor); paintings closer; environment finished "like the Louvre" without upstaging the art; **360° look**; inspect mode = painting centred, card left (desktop) / peek-sheet (mobile), sketch to the right, `Back to the hall` the only button, **"Bring it to life" becomes an Easter egg (tap / zoom the painting)**, top "Esc/Back returns…" chip removed; portrait painting rendered at true aspect; hall-end staircase = stretch.

### Decisions I made for you (veto in the plan review, otherwise they stand)

- **CTA vocabulary unified:** every CTA that leads to `/map` reads `Walk the story` (home, People closer, About closer). Chapter-end stays `Continue`.
- **People closer copy:** eyebrow `Their story lives on` · heading `Stand where they stood` · CTA `Walk the story`. (Kills the walk/walk redundancy Wil flagged.)
- **About closer copy (section 06 rewrite):** eyebrow `The streets are waiting` · heading `Two and a half miles. One day in 1860.` · body `Four bronze plaques mark the route through downtown Troy; the fifth spot, the Ferry Landing, is remembered here on the website. Start at any plaque, or begin from the map. About forty-five minutes on foot.` · CTA `Walk the story`. Distance/minutes are computed from `src/data/route.json` exactly as `src/pages/map.astro` does (2.5 mi / 45 min) — reuse that code, never hardcode.
- **Ch2 part headings lose the em dash by becoming two lines:** `Part 1\nTubman Creates a Crowd`, `Part 2\nThe Mob` (rendered PART 1 / TUBMAN CREATES A CROWD via `<Lines>`); the parser at `[chapter].astro:335` (`scene.label.split("—")`) switches to `split("\n")`.
- **Attribution dashes** (`— Marshal Holmes`, About quote) → dropped; the `.t-meta` figcaption stands alone. Title tags `X — Charles Nalle Walking Memorial` → `X · Charles Nalle Walking Memorial`. `no plaque — website only` → `no plaque, website only`. Ch2 address `5 State Street — Mutual Bank Building` → `5 State Street · Mutual Bank Building`.
- **Mobile /map overview:** one bottom row on the ☰'s axis — attribution (i) at the left inset, `Take the walk` centred in the free lane, ☰ bottom-right (Wil: "center aligned with the hamburger"); `See Troy in 1858`/`Back to today` becomes a compact pill top-right at `--ui-inset` (opposite the date chip; overview-only so it never meets Stop/Continue); geolocate control removed everywhere. Desktop keeps the centred doors pair.
- **Mobile /map focused + walk mode:** `Back` top-left, `Stop the walk`/`Continue` top-right — **identical to desktop** — and the ☰ **hides on phones while a stop is focused** (Wil floated moving it top-right; measured, a 360px top row cannot hold Back 90 + Stop-the-walk 164 + ☰ 72 with insets = 398px; the burger already retreats on scroll and `Back` is the single exit that brings it back; done via a separate `data-walk` attribute on `.cnwm-menu` so `Menu.astro`'s scroll handler can't fight it). Desktop walk mode: `Back to map` top-left at equal top/left inset, `Stop the walk`/`Continue` top-right, ☰ stays bottom-right in its lane; the card strip is padded so no card sits under the ☰. Everything gated by `states.mjs` = zero collisions at 360/390/430/768/1024/1440/1920.
- **Card titles get an authored `name.card` field** in the chapter schema (`Holeur’s\nFashionable Bakery`, `Commissioner’s\nOffice`; others fall back to `name.canonical`), rendered `whitespace-pre-line hyphens-none` — cleaner than reusing the all-caps three-line `name.display`. Document in `docs/v4/NAMING-CANON.md`.
- **Mobile card geometry:** slide width `min(343px, 84vw)`, positive spacing ~12px, `origin:"center"` → ≥ 16px of each neighbour visibly peeks at 360px; neighbours scale to ~0.92 continuously (`detailsChanged`), fully opaque (the edge fade mask goes `≥640px`-only — the fade *was* the "dimming").
- **The curtain announces itself:** `playCover()` dispatches `document.dispatchEvent(new CustomEvent("cnwm:curtain-cover"))` first thing, so the map island (`map.stop()`, cancel flights/route-draw) and the museum (pause rendering, pause video) go quiet under the cover.
- **Walk completion:** after stop 5 the top-right button reads `Walk again` (restarts from stop 1); a manual drag or `Stop the walk` sets a paused state where it reads `Continue` (resumes auto-cycling from the current card).
- **Where to next declutter:** the embed map gets a soft drop shadow (depth), the CTAs (`Continue`, `Get directions`) move **under the map, centred**, the on-map orange label chip becomes a quiet pill (dark ground, cream text, orange numeral only), and the sticky mini-player **collapses to a small pill** (play glyph + time) once the last narrated paragraph has scrolled out of view / the Onward section is in view — three competing oranges become one.
- **Museum Easter-egg accessibility:** no visible button, but the projected painting rect carries an invisible focusable `<button aria-label="Bring the painting to life" / "Let it rest">` so keyboard and screen-reader users keep the capability.
- **Hamburger scroll-hide already exists** (`Menu.astro:206–229`, hides after 240px scrolling down, shows on 24px up). Verify on real Safari/Chrome mobile; tune thresholds if sluggish; keep on all breakpoints and pages.
- **Footer:** layout-only redesign, identical content and links (Wil). A large quiet decorative wordmark is *allowed* if it clears the juror; not required.

---

## PART A — THE LEDGER (every requested change + latent defects found on the way)

Format: **ID · what → where (file:line @ 623b13a) → acceptance.** "All breakpoints" = the v7 matrix in Part B. Nothing is done until re-shot at the relevant viewports and checked into RUN-STATE.

### A0 · Global rules (apply site-wide, verified by instruments)

- **G1 Rag / orphans / widows — zero, at every viewport.** Mechanism, in this order: (a) `text-wrap: pretty` on every prose/meta/body role, `text-wrap: balance` on every heading/quote/eyebrow role (`.t-display`, `.t-title`, `.t-title-sm`, `.t-quote`, `.t-meta` blocks ≥ 2 lines, `.t-meta-body`, `.btn` labels never wrap); (b) authored breaks for display type where balance isn't enough (`name.display` idiom, `<br>` in `map.astro:14`, `people.astro:16`); (c) a build-time `nbsp()` helper in `src/lib/text.ts` that glues the last two words of headings/eyebrows/labels/captions (extend the one hand-authored case at `index.astro:97`); (d) `hyphens: manual`, never auto. Named cases from the review: home description (3 lines at ≥1200), bakery `Captured with Bread in Hand`, `United States Deputy Marshal Holmes` (bakery mobile), map-lens caption on mobile, People H1 (`ONE DAY. A WHOLE / CITY'S CAST.` at ≥1024; `ONE DAY. / A WHOLE / CITY'S CAST.` below), footer wordmark one line, footer disclaimer no runt, every chapter card title never crossing the arrow. **Acceptance:** new `scripts/rag.mjs` (Part B) reports zero runts (single word or ≤ 3 characters alone on the last line of any ≥ 2-line block) and zero 2-word runts on display/title roles across the whole matrix.
- **G2 No clipped letterforms anywhere.** Root cause found: `global.css:994–997` `.lines .line-box { overflow: hidden }` is unconditional and, with `--lh-display: 0.95` / `--lh-title: 1`, cuts descenders (the "J" in barbershop's moral `FOR JUSTICE`; also ch2 moral 1 `INJUSTICE`; risk on every `<Lines>` use: `[chapter].astro:346, 419, 495, 549`). Fix: give `.line-box` descender room (`padding-bottom: 0.18em; margin-bottom: -0.18em`) or clip with `clip-path: inset(0 0 -0.2em 0)` on the moving inner instead of `overflow: hidden` on the box, and drop the clip entirely once `.is-in` lands / under reduced motion / no-JS. **Acceptance:** `rag.mjs` glyph-clip probe (Range rects vs nearest overflow-hidden ancestor) reports zero clips; before/after shot of barbershop `#moral-heading` at 390/768/1440.
- **G3 Contrast — WCAG 2.2 AA everywhere, measured, no design regression.** Text ≥ 4.5:1 (≥ 3:1 for ≥ 24px / bold ≥ 18.66px), UI components/focus ≥ 3:1. Text over imagery/scrims is measured by **pixel sampling** (extend `scripts/contrast.mjs`: screenshot, sample the composited background inside/around each text rect, worst-case luminance) instead of "UNMEASURED". Named cases: home description (`#b7b3ab` 14px on the film — `index.astro:92`, `.t-meta-body`), moral/theme section body `#fed9cc` on scrim `.78` (`[chapter].astro:471–539`), archival interlude credit `.t-meta #ff9770` floating on the photo (`[chapter].astro:382–408`, worst on barbershop), map/embed pills, museum plaque, footer disclaimer `opacity-70`. Solutions must keep the look: deeper/wider scrims, chips under floating labels, `--color-neutral-12` instead of cream-pink where Wil asked for "white", slight blur only where the image is a background not a subject. **Acceptance:** `contrast.mjs` (incl. pixel mode) exits 0 at 390/768/1440 on all routes; axe (Part B) zero color-contrast violations.
- **G4 Tablet parity.** Every fix is verified at 768×1024 and 1024×768 in addition to phone and desktop; no breakpoint gap between `sm/md/lg` behaviours (e.g. People 2-col at `md` inside a still-stacked `.editorial` at `lg` — check it reads well).
- **G5 Em-dash sweep (locked decision 2).** Inventory at 623b13a: chapter JSONs 37 (paragraphs 14 · historicalContext 7 · portal.history 5 · sketchNote 5 · morals.message 3 · labels/address 3), `people.ts` 5 visible, `people.astro` 3, `about.astro` 1 (+ titles/meta), `map.astro` 4, `paintings.astro` 5, `[chapter].astro` 2 on-screen + delimiter at :335, `index.astro` title/meta, `TroyMap.tsx` aria/alt 4, `Museum.tsx` plaque quote attribution. Code comments are exempt. **Acceptance:** `grep -rn "—" src/ --include=*.json --include=*.ts --include=*.tsx --include=*.astro | grep -v "^\s*//\|<!--\|/\*\|\* "` returns only comments; CONTENT-STATUS.md carries the per-string ledger for every locked-prose edit; audio timings untouched (`node scripts/audio-timings.mjs` not re-run — words unchanged).
- **G6 Floating-UI grid.** Everything fixed/absolute honours `--ui-inset` (20/40/56) and the lane doctrine (`global.css:1069–1084`, update the stale comment: on `/map` the menu is bottom-right and attribution bottom-left). Known offenders: AudioStory mini-player `fixed bottom-4 left-4` (`AudioStory.tsx:381`), Museum's three ad-hoc insets. **Acceptance:** `states.mjs` zero collisions in every captured state (add the new states in Part B).
- **G7 Motion tokens.** New motion uses `--dur-fast/--dur-slow/--ease` unless it is one of the documented exceptions; wire the dead tokens (`--ease-circ-in-out`, `--ease-pop`, `--dur-curtain`, `global.css:217–219`) into `curtain.ts`/`Menu.astro` or delete them; document the museum's damped lerp and the new parallax as exceptions in `docs/v4/MOTION.md`. Reduced-motion parity 100%.

### A1 · Home / splash (`src/pages/index.astro`)

- **H1 Move the hero image down so Charles Nalle's whole head is visible above the eyebrow line** (`Troy, New York · April 27, 1860`). Today the `<video>` (`:27–37`, `splash.mp4`) and `<picture>` (`:39–52`, `home-bg-{800,1440}`) are `object-cover` at default `50% 50%`. Port the chapter `heroFocus` idiom (`content.config.ts:87`, `[chapter].astro:213/221`) to home: `object-position: center <N>%` on BOTH video and image, N chosen per orientation (portrait vs landscape; the unused `home-bg-horizontal-*` assets may serve landscape/desktop if the vertical crop can't show the head). **Acceptance:** at 390×844, 768×1024, 1024×768, 1440×900, 1920×1080, 844×390: full head visible with headroom, above the text stack; LCP preload asset unchanged or updated in `Base.astro` accordingly; frame opacity/film unchanged.
- **H2 Description paragraph: three lines at desktop, beautiful rag everywhere, no orphan.** `index.astro:92–97` (`home-desc t-meta-body max-w-[46ch] text-balance`, three hand `&nbsp;`). Widen measure (~56–60ch at ≥1200), keep `text-balance`, `nbsp()` the last two words. Words unchanged (Kathy's sentence). **Acceptance:** exactly 3 lines at 1440/1920; 4–5 balanced lines at phone; `rag.mjs` clean.
- **H3 CTA copy `Walk the five spots` → `Walk the story`** (`index.astro:102`).
- **H4 Mobile: CTA bottom-aligned** — on <768px pin the CTA to the bottom of the frame, full-width within the content inset, **bottom margin = the left/right content inset**; the rest of the stack centres in the remaining height; landscape phones (`max-height:500px` rule at `:119–126`) keep the in-flow layout. **Acceptance:** shots at 360/390/430 portrait + 844×390; tap target ≥ 48px; nothing overlaps the frame border.
- **H5 Description contrast passes AA on mobile/tablet/desktop** — raise the role (16px/1.55, `--color-neutral-12` at ≥ 0.9 opacity, or `--ink`), and/or extend `--scrim-photo` upward (`global.css:200`, starts at 65%) so the pixel-measured ratio ≥ 4.5:1 at every viewport. Keep the film look.
- **H6 Verify** the entry choreography (`:127–172`) still lands the CTA in ≤ 700ms and reduced-motion shows everything.

### A2 · The Crossing / curtain (`src/lib/curtain.ts`, `src/layouts/Base.astro`, `global.css:1132–1149`)

- **X1 Kill the end-of-sequence jitter/flash on every curtain use** (chapter cards → chapter, chapter → Continue → next chapter, menu links). Root cause found: the exit half runs from a **deferred module script** (`Base.astro:93–95` → `<script type="module">` at end of body), so page B paints **uncovered** first, then `gsap.set(panel,{y:0})` snaps the curtain over it, holds 0.45s, then reveals; contributing: no font preloads (FOUT on the wordmark), no `will-change`, `setLabel()` innerHTML + hard `opacity:1` set on the same tick, unconditional park at `onComplete`. Fix set: (a) an `is:inline` blocking `<script>` in `<head>` that reads the `cnwm-curtain` sessionStorage flag and sets `document.documentElement.classList.add("curtain-covered")` + stashes label/date, with CSS `.curtain-covered .curtain-panel{transform:none;will-change:transform}` and `.curtain-covered .curtain-text{opacity:1}` **before first paint** (label text can be written by the same inline script once `#curtain-text-content` exists — or render the date lockup server-side and swap only the label); (b) `<link rel="preload" as="font" type="font/woff2" crossorigin>` for Libre Caslon Display 400 + Text 400 (the two curtain faces) in `Base.astro`; (c) tween the text in on page B (0.2s) instead of a hard set; (d) `will-change: transform` on `.curtain-panel` during the sequence, park on the next frame; (e) `playCover()` dispatches `cnwm:curtain-cover` first thing; the map island (`leaving=true`, `tourRun++`, `map.stop()`, clear `flyTimeout`, cancel the route-draw rAF) and the museum (pause loop + video) go quiet under it — `TroyMap.navigateToStop` (`:839–847`) does the same before calling `playCover`; (f) keep the fail-open timers, keep reduced-motion = instant. **Acceptance:** frame capture (Playwright `page.video` or CDP screencast) of card→chapter and Continue→next at 390 & 1440 with 4× CPU throttle shows **no uncovered frame of page B before the hold**, no wordmark reflow, one continuous 0.6s reveal; `states.mjs` `03-curtain-mid` still clean; Lighthouse unchanged or better.

### A3 · Map + the walk (`src/components/TroyMap.tsx`, `src/pages/map.astro`, `Menu.astro`)

- **M1 Remove the location icon everywhere** — delete the `GeolocateControl` (`TroyMap.tsx:547–554`) on all breakpoints; keep the compact attribution.
- **M2 More 3-D: raise the overview pitch** (`OVERVIEW.pitch` 33 at `:36`; prologue `easeTo` `:676`; `overviewCamera()` `:398–445`). In mapbox-gl 3.27 `cameraForBounds(bounds, {padding, bearing, pitch, maxZoom})` **does honour `pitch`** (verified in `node_modules/mapbox-gl/dist/mapbox-gl.js`) but knows nothing about the DOM label pills — so: for pitch candidates `[52, 48, 44, 40, 36, 33]`, `cameraForBounds` with a safe box (`--ui-inset` + top row + bottom doors), then step zoom down ≤ 1.0 and check every marker **label rect** (phone chips 24×24 with ≥ 22px centre separation; desktop pills via `pinOffset`) is inside the safe box using `map.transform.clone()` + `locationPoint3D` (internal-but-typed; fallback = a synchronous `jumpTo` search restored in the same task); cache per `(w,h)`, recompute on debounced resize; keep the landscape-phone zoom floor. Expected: 1920/1440 ≈ pitch 48–52 @ zoom 15.25; tablets ≈ 44; 390×844 ≈ 36–40 (all five visible — CO/Barbershop chips are the binding constraint). Verify the Studio style actually renders `fill-extrusion` at these zooms (`map.getStyle().layers.filter(l=>l.type==="fill-extrusion").map(l=>l.minzoom)`); if `minzoom` > overview zoom, `map.setLayerZoomRange` at runtime or note for Wil (style lives in his Mapbox account). Apply the chosen pitch to deep-link arrival (`:658–668`) and to the follow camera. **Acceptance:** projected-label check passes at 360/390/430/844×390/768/1024/1440/1920; `map.getPitch() ≥ 40` on ≥1024; before/after shots.
- **M3 `Stop the walk` → top-right** at `--ui-inset` on every breakpoint (today `absolute bottom-44 … sm:bottom-52 z-30` at `:1040–1050`, overlaying the cards); mirrors `Back`/`Back to map` top-left. Never overlays cards or map labels.
- **M4 Manual drag stops auto-cycling; `Continue` resumes.** Replace `touring`+`tourAbort` with `walk: "idle"|"walking"|"paused"` (+ mirror ref), a `tourRun` counter and a `leavingRef`; `runTour(from)` is an abortable async loop that checks `run === tourRun.current && !leaving` before every step (a stale sleeping iteration wakes, fails, returns — no double-drive, no yank); `pauseWalk()` (= `tourRun++`, `map.stop()`, `walk="paused"`) is called from keen `dragStarted`, inactive-card tap, marker tap, keyboard nav; `continueWalk()` = `runTour(activeIdx)`; `stopWalk()` = `tourRun++`, `map.stop()`, `walk="idle"`, short `easeTo` to the current stop (never leave the camera frozen mid-arc). Button label: `Stop the walk` (walking) / `Continue` (paused, `aria-label="Continue the walk"`) / `Walk again` (after stop 5, restarts from 1). **Acceptance:** Playwright: start walk → drag → `walk==="paused"` immediately, keen `track.details.position` sampled every 16ms for 400ms shows no reversal > 0.03 (no snap-back), button reads Continue; press → cycling resumes from the current card; Stop → `map.isMoving()` false ≤ 1.3s.
- **M5 Carousel drag smoothing — root cause found:** keen-slider 6.8.6 `mode:"snap"` release is **hard-coded** to `animator.start({duration:500, easing: 1+(t−1)^5})` and advances a card on ANY non-zero release velocity — a 5px nudge flings a whole card in a fast quintic; `defaultAnimation` only affects `moveToIdx()`; and the reconciliation effect's 80ms `moveToIdx(…,{duration:0})` retry (`:793–798`) fires while a programmatic move is 17% done → every tour step is a jump-cut and any `setActiveIdx` during a live drag yanks the strip. Fix (`:757–800`): keep `mode:"snap"`; in `dragEnded` (which runs AFTER keen's snap plugin, so a `moveToIdx` there **replaces** its animation) compute the target = nearest snap point, or `start ± 1` for a real flick (`|track.velocity()| > 0.0008`), **never more than ±1 card**, and `moveToIdx(target, true, {duration: 650, easing: expoOut})` (≈ `--ease`); if `idxToDist(target)===0` settle immediately (keen emits no `animationEnded` for a zero-distance move); `slideChanged` only updates the active marker (never flies mid-drag); `animationEnded` (+ an 800ms settle timer fallback) → `settle()` → `followCamera(idx)` = `easeTo({center, zoom 17.75, pitch, duration 1100, easing expoOut, offset lift})` (short hop between neighbours; `flyTo` stays for marker taps from the overview); the reconciliation effect early-returns while `dragRef` or `animator.active`; `dragSpeed` 1 (try 0.85 only if still too fast); per-slide scale in `detailsChanged` (allocation-free; `transform`/`zIndex` writes only). Under reduced motion `defaultAnimation.duration = 0`. **Acceptance:** 60fps CDP trace during a 5-card drag at 390 and 1440 (no long tasks > 50ms); the fresh juror judges "feels natural" on a screen recording.
- **M6 Card titles on two lines:** add optional `name.card` to the schema (`src/content.config.ts`), `map.astro:24` `cardTitle: name.card ?? name.canonical`, render `whitespace-pre-line hyphens-none` (`TroyMap.tsx:1140–1142`); measured at ≈ 0.48em/char Caslon, `Fashionable Bakery` sits on one line at all three card sizes; `Uri Gilbert Home`, `Washington Street Ferry Landing`, `Peter Baltimore's Barbershop` unchanged (may wrap naturally but must never cross the arrow decoration `:1146–1157`). Card title becomes a `.t-*` role, not raw `text-[1.125rem]`. **Acceptance:** shots of all five cards at 343 / 428.75 / 514.5px; bakery/CO title `getClientRects().length === 2`; title bottom ≤ arrow row top.
- **M7 `Back to map` equal inset** — `top-[calc(var(--ui-inset)+48px)]` (`:998`) → `top-[var(--ui-inset)]` (the chip is hidden in focused mode anyway); left already `--ui-inset`.
- **M8 Mobile overview layout** (<640px): bottom row = attribution (i) at the left inset, `Take the walk` centred in the free lane between the (i) and the ☰ (bottom-right), all on one axis at `bottom: calc(var(--ui-inset) + 12px)`; `See Troy in 1858`/`Back to today` becomes a top-right pill (overview-only, so it never meets Stop/Continue); geolocate gone (removing it leaves attribution alone in `.mapboxgl-ctrl-bottom-left` — recheck `global.css:869–884` spacing); the bottom flex-wrap row (`:1015–1035`, `bottom-8`) is replaced. Desktop keeps the doors pair centred at the same axis. **Acceptance:** `states.mjs` at 360/390/430: zero collisions; every control ≥ 44px tall; the button fits the 360px lane (≈ 189px in ≈ 200px).
- **M9 Mobile walk cards:** fully opaque (`.location-cards-slider` edge fade `global.css:1007–1022` becomes `≥640px`-only), inactive cards continuously scaled ~0.92→1.0 by slide position (`detailsChanged`), previous/next card edges visibly peek ≥ 16px at 360px (slide width `min(343px, 84vw)`, positive spacing ~12px, `origin:"center"` — today spacing is **−20**, i.e. overlap), map follows the active card on swipe via the M5 settle path (covers drags that end on a snap point with no animation), CTA text `Back` (`<span class="hidden sm:inline">to map</span>`, `aria-label="Back to map"`), ☰ hidden on phones while focused (`data-walk`), `Stop the walk`/`Continue` top-right, card strip `pb-[calc(var(--ui-inset)+32px)]` so it clears the attribution. **Acceptance:** Playwright touch-drag at 390 (`hasTouch`): after each 120px swipe `map.getCenter()` ≈ the active stop within 40 m; a slow 20px drag returns to the same card and the map does not move; shots show peeks on both sides; `states.mjs` clean.
- **M10 Chip copy** `Five spots · April 27, 1860` → `April 27, 1860` (`:956`).
- **M11 Card spacing on mobile** balanced (part of M9).
- **M12 Curtain interplay:** `map.stop()` before `playCover()` (`:839–847`) — see X1.
- **M13 Map page copy block** (`map.astro:54–123`): fix rag of heading/prose at all viewports; em dashes (`:65`, `:114`); `Spot NN` zero-padding consistent site-wide (`paintings.astro:169` → pad2).

### A4 · The 1858 lens (inside `TroyMap.tsx:203–335` logic + `:867–945` markup — it is an overlay, not a page)

- **L1 Reframe (locked decision 1):** initial `lensView` and `lensReset()` compute a transform that fits the **lower panel** of the 4096×3431 plate (measure the split line's y-fraction on the asset once and store it as a constant, e.g. `LOWER_PANEL = {y0: 0.5x, y1: 1}`), scaled to fill the viewer; clamp panning so the whole plate remains reachable; scale range keeps 6× at street legibility.
- **L2 Bigger viewer:** `width: min(92vw, 88dvh)` + `aspectRatio 4096/3431` (`:890`) → the viewer fills the map shell (near-full-bleed within `--ui-inset`; on desktop ≥ 80% of the viewport area), no quality degradation (`troy-1858-full-4096.avif/webp` unchanged; consider a `-6144` tier only if the reframed initial view is < 1 asset px per CSS px at 1920).
- **L3 Copy/controls in the lens:** remove `Take the walk` from the lens view (only `Back to today`, centred); the top chip reads `April 27, 1860` (M10) or is hidden while the lens is open (Wil: "remove the words five spots"); fix spacing/rag of `Troy, New York · 1858 · Library of Congress` + `Drag to explore · pinch or scroll to zoom` under the viewer at all viewports (mobile rag named by Wil).
- **L4 Interaction unchanged** (pointer-capture pan, pinch, non-passive wheel, keyboard `+ − 0 arrows`); reset button re-frames to the lower panel.
- **Acceptance:** shots at 390/768/1024/1440/1920 open + zoomed; keyboard path works; `states.mjs` new state `map/06-lens-open` collision-free.

### A5 · Chapter template — all five pages (`src/pages/[chapter].astro`, `AudioStory.tsx`, `EmbedMap.tsx`, `ChapterSpine.astro`, `Lines.astro`, `global.css`)

Render order today: hero → (Part-2 painting hero) → story (`#scene-i`: heading + quote + AudioStory) → interlude (archival photo + credit) → `#history` (cream `<dl>` + portal prose) → moral(s) → `#onward` → footer. Rhythm today (≥1200): 128 · 128 · 128 · 0 · 0 · 128 · **400 (+160 moral padding = 560)** · 200.

- **C1 Drop cap:** enlarge the first letter of the first story paragraph of each part — storybook style, not decorated. Attach at `AudioStory.tsx:326–332` (`items[0]` is always a `p` in all five chapters): a class on the first `.t-prose` + `::first-letter` (`initial-letter: 3` with a `float` fallback, Libre Caslon Display, `--ink`, kerned; must not break the `narration-active` highlight or tap-to-seek; reduced-motion irrelevant). **Acceptance:** shots of all six openings (ch2 has two) at 390/768/1440; the drop cap sits on 3 lines at desktop, 2–3 at phone; no orphan created.
- **C2 Moral/theme contrast + parallax:** section `:471–539`. Body `.t-prose` `#fed9cc` → `--color-neutral-12` (Wil: "white like the heading"); scrim `linear-gradient(#1d1411, rgba(29,20,17,.78) 30%, .78 70%, #1d1411)` → deepen the middle band to what the pixel measure needs (~.84–.88), optional slight blur ONLY on the background image (`filter: blur(1.5px)` on the `<img>`, never on the sketch); measured ≥ 4.5:1 for body and caption at every viewport. Add **scroll parallax** to the background image (`:474–479`): translateY ±6–8% travel bound to scroll progress, `ease:none` (scrub), rAF/ScrollTrigger, disabled under reduced motion, image oversized to avoid edge reveal, no CLS. Cream heading unchanged.
- **C3 Artist's study centred:** `<figure class="… md:grid md:grid-cols-[minmax(0,420px)_1fr] md:items-end">` (`:506–527`) → `md:items-center` (sketch vertically centred with the caption/text block); check the same alignment idiom in Museum plaque and About portraits.
- **C4 Archival interlude label contrast + "good loading animation, no parallax":** the credit `.t-meta` `#ff9770` floats on the photo (`:401–403`) — put it on a chip (`color-mix(primary-2 82%)` like the map chip) or under a bottom scrim band; the interlude keeps its `.wipe` reveal (verify it fires at natural scroll speed at every viewport, incl. the a651e35 clip fix) and gets **no** parallax. Barbershop named as the worst case. **Acceptance:** pixel contrast ≥ 4.5:1 for the credit on all five interludes at 390/768/1440.
- **C5 Where to next declutter** (`:543–599`, `EmbedMap.tsx:34–58`, `global.css:794–817`): embed map gets a soft shadow (`0 24px 64px -24px rgba(16,10,6,.7)`-class, on-token), CTAs centred under the map, next-title/address block centred or left-aligned consistently, orange chip on the map → quiet pill (dark ground, cream label, orange numeral), mini-player collapses to a compact pill when Onward is in view (see decisions; `AudioStory.tsx:379–408`, `mainVisible` logic `:144–153`) — result: one primary orange (Continue). Same at all breakpoints. **Acceptance:** shots 390/768/1440; a fresh juror names `Continue` as the single primary action.
- **C6 Rhythm:** (a) part heading → opening quote gap `.beat` (128 desktop, `:349`) reduce to `--space-block`×2 / `--sp-5` (≈ 48–64) so the quote reads as the heading's companion (Wil: RIVER ESCAPE → quote too far); apply identically in all five (and both ch2 parts); (b) moral bottom → Onward top today = 160 padding + `.void` 400 = 560px ≈ a whole screen (Wil: users think the page ended) → Onward `void` → `sec` (200) with the moral's bottom padding tuned so the total ≈ 200–260 at desktop, proportionally at phone/tablet; (c) re-check every gap quantizes to a token; (d) Uri Gilbert Home and all others share identical section spacing. **Acceptance:** `census.mjs` rhythm table shows the same ladder on all five pages; shot of ferry `RIVER ESCAPE`→quote and moral→Onward before/after at 390/768/1440.
- **C7 Hamburger scroll-hide + hero on mobile:** verify hide-on-scroll-down/show-on-scroll-up works on device (Menu.astro:206–229) and that on mobile the chapter hero focus keeps faces clear of the ☰ (bakery named: Charles's face under the burger) — per-orientation `heroFocus` (schema `content.config.ts:87`, allow `{portrait, landscape}` or a `heroFocusMobile`), and fix the latent bug that the **portrait `<video>` at `:229–237` has no `object-position`** while its poster does.
- **C8 Barbershop (ch5):** (a) hero focus up to the faces (`heroFocus` 62 → ~40–48, verify at all viewports; the vertical-poster branch too); (b) story body T→I→T→I→T: move `@media:narrative2` from `paragraphs[2]` to after the "Across his body…" paragraph in `src/content/chapters/barbershop.json` (indices `[0]T [1]@media:narrative1 [2]T [3]@media:narrative2 [4]T`; timings unaffected — media rows are skipped by `AudioStory.tsx:87–93`); (c) archival label contrast (C4); (d) the clipped J (G2); (e) study centred (C3).
- **C9 Chapter 2 reorder (locked decision 6):** interleave the three loops in `[chapter].astro` (`:279` scenes, `:382` interlude, `:411` history, `:471` morals) into one ordered render list for chapters with 2 scenes: Part 1 → interlude → History → Moral 1 → Part-2 painting hero → Part 2 → Moral 2 → Onward; **push into `sections` (`:139–150`) in the same order** so the spine numerals `(01)…(06)` and the collapsed mobile spine agree with scroll order; each part keeps its own `AudioStory` (own mp3, own timings) — but only ONE mini-player may be visible at a time (the most recently played; the other's mini hides) at a single `--ui-inset` slot. **Acceptance:** rendered id order `hero → scene-0 → interlude → history → moral-0 → hero-2 → scene-1 → moral-1 → onward`; spine reads Listen·Pt 1 / History / The moral·Pt 1 / Listen·Pt 2 / The moral·Pt 2 / Onward; both audio players work; highlight + tap-to-seek verified on both (timings need no re-run — words unchanged).
- **C10 Chapter footer** reserves the mini-player lane (`pb-28`) — recheck after C5's collapse behaviour.
- **C11 Em dashes in chapter UI** (`Next — Spot NN` `:570`, `— attribution` `:351`, alts/titles) and JSON labels/address per decisions; the `split("—")` parser at `:335`.
- **C12 Latent:** `mansion.json` audio subtitle `Uri Gilbert Mansion` → `Uri Gilbert Home` (UI subtitle only; log it); `[chapter].astro:153` duplicate `longestLine` → import from `src/lib/text.ts`; `fitChars()` dead export → use it or delete.

### A6 · Footer (`src/components/SiteFooter.astro`, on 404/paintings/about/map/people/chapters; NOT home)

- **F1 Redesign for a high-end finish, same content and links.** Reference award-winning footers (Awwwards SOTD memorial/museum sites; the executor looks at 5–8, writes a 10-line note in `docs/v7/AUDIT.md`), then compose: full-bleed hairline; desktop 3-column grid — wordmark block (`Charles Nalle Walking Memorial` **one line, `white-space:nowrap`**, role `.t-meta` or `.t-meta-body` bold) + `Made by Notable`; nav as a clean vertical list with the arrow idiom (`The people of this day` / `The paintings` / `About the memorial` / `Map`, current page hidden); `Share` on chapters right-aligned; second row `rule-top`: disclaimer `Walking routes and directions are suggestions only. Explore at your own pace, discretion, and risk.` (`max-w` tuned + `nbsp()` so no runt at 390/768/1440) — mobile stacks in that order. A large quiet decorative wordmark is permitted if the juror keeps it. **Acceptance:** shots at 390/768/1024/1440/1920 on a chapter (with mini-player) and on `/about`; `rag.mjs` clean; contrast ≥ 4.5:1 for the disclaimer (drop `opacity-70` if needed).

### A7 · Menu / hamburger (`src/components/Menu.astro`)

- **N1 X spins on close** — the panel's close button (`Icon name="close"`, `:70–78` / `:118–126`): on click rotate 90°→(180°) over `--dur-fast` `--ease`, then `close()`; also acceptable to morph the burger→X on open (Wil: "micro-animations are cool"), but the required item is the close spin; reduced-motion = no spin.
- **N2 Scroll-hide/show** verified on device at all breakpoints/pages (thresholds 240px/24px at `:206–229`); never hides while open; also on `/map` (page scrolls under the map) and `/paintings`.
- **N3 Arrow tail** stays as is (Wil); if a better tail is found in F1/G-work, propose it in the REVIEW-GUIDE, don't ship it silently.

### A8 · The People (`src/pages/people.astro`, `src/data/people.ts`)

- **P1 Remove the spot links under every person** (`people.astro:105–116`, `Spot {orderFor(slug)} · {shortFor(slug)}`); keep monogram + role + name + note. The `chapters` array in `people.ts` stays in data (may serve later) but renders nothing.
- **P2 Closer copy** (`:128–146`): eyebrow `Their story lives on` · h2 `Stand where they stood` · CTA `Walk the story` (was `Their streets are still here` / `Walk the route` / `Walk the five spots`); rail `(03) ONWARD` unchanged; the section number is hardcoded `(03)` — fine.
- **P3 Em dashes:** `people.ts:19,48,72,81,87`, `people.astro:27,33,41` → punctuation-only rewrites (these are Kathy-confirmed notes: log in CONTENT-STATUS).
- **P4 H1 rag:** `ONE DAY.<br />A WHOLE CITY’S CAST.` (`:16`) → authored breaks per breakpoint so it reads `ONE DAY. A WHOLE / CITY'S CAST.` at ≥1024 (Wil's reading) and never leaves `CAST.` alone; `fitChars` recomputed per variant.
- **P5 Grid check** at 768–1023 (2-col cards inside stacked editorial) and the header two-column at `lg`.

### A9 · About (`src/pages/about.astro`, `src/data/about.ts`)

- **A1 Portraits → closing quote spacing:** the quote section is `.void` (260/320/400, `:120`) after Scott Christianson's `.sec` — reduce to `.sec` **and** make it a proper numbered section (`.sec-head` hairline + rail `(06)` label e.g. `Afterword`); Onward becomes `(07)` (its numeral is derived: `people.length + 3` → +4). Also confirm the inline `--fs-quote` override (`:126`) still reads well.
- **A2 Section 06/07 copy rewrite** per decisions (eyebrow `The streets are waiting`, heading `Two and a half miles. One day in 1860.`, the informative body sentence, CTA `Walk the story`), computed distance/time from `route.json`.
- **A3 Em dash** in the quote attribution (`:130`) dropped; title tag `·`.
- **A4 Latent:** `about.ts:24` dead kicker `On the sidewalk` — either render it or delete; portraits `250×251` upscaled to `w-[250px]` are fine, note it.

### A10 · The Museum (`src/components/Museum.tsx`, `src/pages/paintings.astro`) — the heavy lift

Facts: `SPACING 7`, `CORRIDOR_HALF 3.4` (2.4 portrait), `EYE 1.55`, camera `fov 58/72, far 60`, `fog 10→40`, end wall z −78 and the "destination" glow z −77.8 (both beyond far/fog — the glow has never rendered from the entrance), rail ends z −67 (3 m short of the last work at −70), rail pitch always 0, drag-look clamps yaw ±0.6 / pitch ±0.3 (side-wall art unreachable by look), approach = camera dolly (desktop: canvas in the right 50%, plaque column 44% left; phone: centred, `yOff −.3`; on phones `dist` can exceed the corridor — it works only because every wall is a single-sided `PlaneGeometry`, so **any new wall-length geometry must stay a FrontSide plane**), plaque = DOM card bottom-left `max-w-[46ch]` with a border and two buttons, `MeshBasicMaterial` + baked fake lights, frames = 2 nested boxes, **every canvas plane is 3.0×2.0 because `paintings.astro:78` hardcodes `aspect: 1.5`** (real: six 1440×810 = 1.778, three 1440×960 = 1.5, and `barbershop/narrative2` 1440×2160 **portrait 0.667 — the "sheriff's office" distortion**; its video is 800×1200 portrait too), sketches also hardcoded `sw = sh*1.45` (real 1.21–1.59), 2-D grid tiles `aspect-[3/2]`, the sketch is hung on the **entrance side** of its painting so on left-wall works it appears screen-LEFT (M6 needs `sz = z + side*(…)`), `turnOn(i)` = `VideoTexture` swap, slot `(n+1)*100vh`, `matchMedia` in render, `visibilitychange` bug, three ad-hoc insets, **on `/paintings` the corner menu is TOP-RIGHT (Base default) — the current "Skip the hall"/"Skip ↓" pills sit in its lane (a live collision masked only by the burger's scroll retreat)**, dot rail hidden in approach, `↓` literal glyph, `/paintings` absent from `perf.mjs`, no museum states in `states.mjs`.

**Design notes (from the design pass — the executor may deviate with a logged reason):** `SPACING 5`, `OVERRUN 1.5` (rail passes the last canvas), `END_GAP 6` (end wall visible over the last ~25 m), an **entry wall at z=+7** with a doorway so the 360° look has something behind you, `RAIL_PITCH −0.10 rad` (−0.08 portrait), camera `far 80`, `Fog(8, 32)`, slot `≈ 9vh-units` (keep `vh` — `dvh` would move the slot as the URL bar collapses; compute `total` from `stage.clientHeight`), damping τ ≈ 0.22s dolly / 0.16s look; painting sizing `landscape: h 2.0, w ≤ 3.6 · portrait: h ≤ 2.6 (2.3 phones), w ≤ 3.2, yC 1.6`; sketch `sw = sh × sketchAspect`, hung screen-right on both walls; approach composition = pure `compose(work, layout, ui, zoom)` recomputed every frame (so sheet drag + zoom recompose through the lerp): horizontal field `F = 1 − 2·max(L,R)` centred (desktop triptych ≈ card 30% / painting 40% / sketch 30%), vertical field respects the top row + dot rail (or the live sheet top), `d = max(w/(2·tanH·F), h/(2·tanV·V))` capped at `2·CH − 0.15` (widen `fov` up to 84° instead of leaving the corridor), `dEff = d / zoom`, `zoom ∈ [1, 2.4]` with an **edge-triggered** alive rule (`≥ 1.35` turns on once, `≤ 1.20` turns off and re-arms; a tap toggles regardless); look = unbounded yaw (wrap), total pitch clamped `[−0.55, 0.50]`, sensitivities 0.0035/0.0025 rad/px, inertia EMA of the last 3 samples decaying with τ 0.18s, `Recenter`/`Face forward` chip when `|yaw| > 0.35`, double-tap on wall/floor recentres, `←/→` = ±0.35 rad in rail / prev-next work in approach, `+/−` zoom, `Esc` back; `touch-action: pan-y` on the canvas in rail, `none` in approach; tap discriminator `< 300ms && < 8px`; **finish = `MeshBasicMaterial` + richer baked `CanvasTexture`s** (plank floor with per-board jitter/grain/sheen band aligned to `SPACING` — the strongest forward-motion cue; plaster noise walls; coffered ceiling with soft disc highlights; 0.16m baseboard + cornice as FrontSide planes; frames = four nested boxes with per-face material arrays so top faces read lit: moulding primary-7, gilt lip `#8f7040`/top `#ad8950`, slip `#302414`, rebate `#100a06`; blue-noise dither on every gradient; dim/shrink the floor pools) — budget ≤ 80 draw calls in view, ≤ 90 MB textures desktop / 35 MB phone, zero per-frame allocations, 60fps at DPR 1.5; **desktop card** `absolute left-[--ui-inset] top-1/2 -translate-y-1/2 w-[min(30vw−inset−24px,22rem)]`, no border, one button + an `sr-only focus:not-sr-only` play/pause toggle; **mobile sheet** = pointer-drag header (`touch-action:none`, `setPointerCapture`, rAF-throttled `transform`, velocity > 0.3px/ms decides, settle via `transition: transform var(--dur-fast) var(--ease)`; peek = header height ≈ 96–118px, full ≤ 0.55·stageH with a scrolling body; `Back to the hall` as a top-left `btn-sm btn-ghost` so it is always reachable; the dot rail rides above the sheet) — no nested overflow container (it would steal the tap/pinch and chain scroll into the page rail); landscape phone hides the quote and puts Back top-left; **debug hook `window.__museum`** (`state`, `approach(i)`, `setLook`, `recenter`, `setZoom`, `setSheet`, `paintingRect(i)` projected to CSS px, `renderer.info`) drives `scripts/museum-check.mjs`; **hall-end threshold** = a doorway in the end wall + three step boxes descending toward the lowered glow (4 meshes). Pitfalls: `VideoTexture` `play()` may reject under iOS Low-Power — retry on the ending `pointerup`/click, pre-create `<video preload="auto">` on approach; treat `pointercancel` as `up`; wheel listener on the stage element `{passive:false}` never `window`; horizontal-trackpad look needs `overscroll-behavior-x: none` on `<html>` while mounted (macOS swipe-back) — ship behind a flag after a Mac check; dispose the per-face material arrays and the new textures; pause videos on `pagehide` and under `cnwm:curtain-cover`; layout (column/sheet) is live via ResizeObserver while the world stays as built (note it); the corner menu owns top-right on `/paintings` — keep that lane empty in every museum mode.

- **U1 Camera pitched down a little in rail mode** (target pitch ≈ −6° to −8°, floor visible), paintings still framed as you pass; eye height stays ≈ 1.55.
- **U2 Paintings closer:** `SPACING` 7 → ~5 (hall ≈ 30% shorter; slot height scales), rail reaches and slightly passes the last work; `camera.far`/`fog.far` raised so the end wall + draw-glow are visible from the start; texture memory budget re-checked (more canvases in view; keep the 800/1440 tiers, DPR ≤ 1.5).
- **U3 Finish the environment (Louvre feel, art stays the subject):** bevelled/moulded frames (3–4 nested boxes, lighter inner lip, warm gilt from the palette), floor material (procedural canvas texture: wide planks or stone with faint grain and baked highlight bands), baseboard + cornice line, ceiling with soft coffers/recessed-light discs, faint plaster noise on walls, optional benches; **stay on `MeshBasicMaterial` with richer baked textures** unless a measured 60fps budget on a mid phone survives `MeshStandardMaterial` + 2–3 `SpotLight`s (shadows off) — decide by trace, document in `docs/DEVIATIONS.md`/`MOTION.md`.
- **U4 360° look:** unclamp yaw (full turn), pitch clamp ≈ ±0.5 rad; drag/touch-drag with light inertia; look offset persists but a double-tap / `Recenter` control or any approach resets it; no mouse-move look (accidental).
- **U5 Movement feel:** keep native scroll (no scroll-jacking); the downward pitch (floor motion cue) + consistent damping + keyboard walking (`↑/↓` or `W/S` walk, `←/→` look, `Enter` approach, `Esc` back) + a subtle floor/threshold cue; horizontal trackpad deltas → look is optional; the rail counter and dots visible in every mode incl. approach.
- **U6 Inspect mode, desktop:** painting **centred** in the frame; the info card at the **left edge**, ~30–34% column, vertically centred or bottom-left, **no border**; sketch hung to the **right** of the painting and visible; buttons: only `Back to the hall`; the "Esc/Back returns…" chip removed (Esc still works); **tap/click on the painting toggles the animation** (also: wheel/pinch zoom-in past a threshold turns it on, zoom-out or tap again turns it off) — invisible focusable button over the projected painting rect for keyboard/SR; the plaque copy (`Mark Priest · Nalle Series · Chapter n`, title, quote) unchanged apart from the em dash.
- **U7 Inspect mode, mobile portrait:** bottom **peek-sheet** (eyebrow + title visible, ~96–120px) that drags/scrolls up to the full card (painting recomposes smaller/higher as the sheet rises) and back down; only `Back to the hall` (as a top-left ghost button so it is reachable in both sheet states); tap the painting to bring it to life; landscape phone uses the desktop (column) composition with the quote hidden. Implement as a pointer-drag sheet with a light spring (design notes) — **not** a nested overflow container (it would steal the tap/pinch and chain scroll into the page rail); no scroll-jacking.
- **U8 True aspect per work:** compute in `paintings.astro` at build (`sharp` `metadata()` on `public/media/<slug>/<key>-1440.jpg` and `sketch-800.webp`, module-level cache, fallback 1.5), thread through `Work.aspect` + new `Work.sketchAspect`; plane fits within a max-height AND max-width box (see design notes) with the frame following; sketch offset uses the real width; VideoTexture cover-fit uses the real aspect (portrait video for the portrait work); the 2-D grid tile uses `style="aspect-ratio: <real>"` (portrait tile is index 9, last row).
- **U9 End of hall (stretch, only after U1–U8 land):** the doorway + descending steps toward the glow that visually leads down into the 2-D "all paintings" section following the sticky stage; cheap geometry; keep the scroll handoff and `Skip the hall`.
- **U10 Hygiene:** every floating element on `--ui-inset` (+ `env(safe-area-inset-top)`), the Skip pill moves **top-left** (top-right belongs to the corner menu on this page), the rail-mode chip reads `The Museum · scroll to walk · drag to look · tap a painting` (phone: `Scroll to walk`) and turns into the `Face forward` button when looked away, `↓` → `ICONS.arrow` rotated 90°, `matchMedia` out of render, `visibilitychange` split into `inView && visible` (reset `lastT` on resume), dispose on leave, `/paintings` added to `perf.mjs` routes, museum states added to `states.mjs` (rail-rest, looked-away, approach-desktop, approach-tablet-landscape, approach-phone-peek, approach-phone-expanded, alive, with the corner menu forced visible), 2-D fallback grid mirrors copy/aspect changes (the grid `<dialog>` already plays the animated variant on open — that is the fallback for tap-to-toggle; no "Bring it to life" wording anywhere).
- **Implementation order (each step tested via `scripts/museum-check.mjs` reading `window.__museum` at 390×844, 844×390, 768×1024, 1024×768, 1440×900; Chromium `--use-gl=angle --autoplay-policy=no-user-gesture-required`):** 1 build-time aspects (U8) → 2 constants + rail + entry wall + sketch side + plane sizing (U1/U2) → 3 debug hook + IO/visibility + `pointercancel` + `touch-action` plumbing → 4 look controller + keyboard + Face forward (U4/U5) → 5 approach composition + zoom + tap toggle + desktop card (U6) → 6 sheet (U7) → 7 finish pass (U3, with fps proxy ≤ 3 frames > 26ms in 240 and `renderer.info.render.calls ≤ 80`) → 8 lanes/insets/copy (U10, `COLLIDE()` zero incl. 360×800) → 9 threshold (U9) → 10 docs (`docs/v4/MOTION.md` exceptions: museum damping τ, approach-mode modality).
- **Acceptance:** juror bar (Part E) — the Museum must still be named as the moment they'd retell; 60fps trace on the rail and in approach at 390 (DPR 1.5) and 1440; zero console errors; keyboard path complete; reduced-motion → grid; perf ≥ 80 desktop / ≥ 70 mobile, a11y 100 on `/paintings`; `paintingRect(i)` centred within ±3% and disjoint from the card/sheet in every approach layout; portrait work hangs tall and narrow.

### A11 · Favicon + `<head>` (`public/`, `src/layouts/Base.astro`, `scripts/build-og.mjs`)

- **I1 The mark:** interlocked **CN monogram in Libre Caslon Display**, converted to outlines. Pipeline: `opentype.js` (devDep) reads `node_modules/@fontsource/libre-caslon-display/files/libre-caslon-display-latin-400-normal.woff` (WOFF is supported; WOFF2 is not) → `getPath()` for `C` and `N` → compose in a 128-grid SVG (dark ground `--color-primary-2` with `rx` matching `--r-lg` proportion, hairline `--color-primary-6`; C in `--color-neutral-12`, N in `--color-primary-9` — the current two-tone reads well on both light and dark tab bars) — the interlock is the design work: the N's left stem nests inside the C's aperture, both letters kept whole; **3 candidate compositions rendered on `/styleguide` at 16/32/180px, fresh juror picks**. Must read as CN at 16px (test: render 16px PNG, inspect at 1× and 8× nearest-neighbour).
- **I2 The set:** `favicon.svg` (paths only), `favicon-16.png/32/48`, `apple-touch-icon.png` (180, no transparency, slightly larger safe-area), `icon-192.png`, `icon-512.png`, a real multi-size `favicon.ico` (16+32+48; `png-to-ico` devDep or hand-assembled), `site.webmanifest` (name/short_name/theme_color `#1d1411`/background_color/icons with **base-path-correct URLs** — generate at build or use relative paths; `start_url` must include the GH Pages base). Script: `scripts/build-favicon.mjs` (sharp for rasters), outputs committed to `public/`.
- **I3 `<head>` wiring** (`Base.astro:44–69`): `rel=icon svg` + `rel=icon png sizes=32x32` + `rel=apple-touch-icon` + `rel=manifest` + keep `theme-color`; all via `withBase()`. Delete the fake `public/favicon.ico` (a 32×32 PNG) in favour of the real one; add `og:image:width/height/alt`.
- **I4 Latent:** `scripts/build-og.mjs` throws ENOENT (reads `@fontsource/martel-sans` + `poppins`, both gone) — port it to Caslon, regenerate `public/og.png` (optionally featuring the new mark), keep 1200×630.
- **Acceptance:** live tab icon at 16/32 on Chrome/Safari/Firefox; iOS home-screen icon; `curl` 200 for every icon URL under the base path; Lighthouse best-practices unchanged.

### A12 · Latent defects (found in exploration — fix while there, cheap)

- G-L1 Home `<title>`/meta em dashes; `/styleguide` must carry `noIndex` (verify), is outside the juror's scoring, but its visible specimens join the em-dash sweep for consistency.
- G-L2 Docs drift: `docs/v4/DESIGN-STANDARDS.md` §1 (Martel/Poppins, 116px) and §7 (1.5px round caps) vs shipped Caslon/88px/1.3px butt-miter; `Icon.astro:4` comment; `global.css:1072–1084` lane comment; `TroyMap.tsx:48–50` stale `pinPosition` comment — update in the docs pass.
- G-L3 Trailing-slash URLs 404 on GH Pages (`/bakery/`): add a tiny inline script to `404.astro` that strips a trailing slash and redirects when the target exists (cheap UX insurance for typed URLs).
- G-L4 `WalkProgress.astro:53` sr-only string still uses `—`; `paintings.astro` `data-title`/alts.
- G-L5 `[chapter].astro` `quote.source` and `mansion` `portal.hook` are authored but never rendered — leave, note in REVIEW-GUIDE (content decision for Wil).

---

## PART B — THE AUDITS (identify before you fix)

**Matrix (v7):** phone 360×800 · 390×844 · 430×932 · 844×390 (landscape) — tablet 768×1024 · 1024×768 — desktop 1280×800 · 1440×900 · 1920×1080. Instruments that are heavy (probe/states/census) run the classic 5 (390, 844×390, 768, 1024, 1440); shots/rag/a11y run all 9; contrast at 390/768/1440; perf on all routes incl. `/paintings`. Routes: `/`, `/bakery`, `/commissioners-office`, `/mansion`, `/ferry`, `/barbershop`, `/map`, `/people`, `/paintings`, `/about`, `/404`.

**Existing instruments (`scripts/`, all `--base http://localhost:4321`, dev server via `astro dev --background`):** `shots.mjs` (matrix — extend viewports to 9), `probe.mjs` (type ladder, floating collisions, alignment, CLS/overflow/tap), `states.mjs` (interaction states + collisions — **add**: map walk-mode desktop/mobile, map paused, lens open, mobile overview, museum rail/approach-desktop/approach-mobile-peek/expanded/alive, menu open on every page, mini-player collapsed), `census.mjs` (type + rhythm ladder), `contrast.mjs` (WCAG; **add** pixel-sampling mode for text over imagery + 768 viewport), `perf.mjs` (Lighthouse mobile; **add** `/paintings`, add `chrome-launcher` to devDeps explicitly), `arrival.mjs` (QR filmstrip), `moments.mjs`, `strip.mjs`.

**New instruments (build in Phase 0/1, clone `shots.mjs` patterns):**
1. `scripts/rag.mjs` — for every visible text block (roles `.t-*`, `.btn`, card titles, footer, plaque, chips): line count via `Range` rects, last-line word count + width ratio, flags runts (1 word / ≤3 chars on the last line of ≥2-line blocks; 2-word runts on display/title/quote), plus **glyph-clip detection** (Range client rects vs nearest `overflow:hidden`/`clip-path` ancestor box, bottom overhang > 1px). Output `docs/v7/qa/rag.{json,md}` per route × viewport. Force reveals into `is-in` first (as `contrast.mjs` does).
2. `scripts/a11y.mjs` — Playwright + `axe-core` (already in `node_modules` via Lighthouse; inject `node_modules/axe-core/axe.min.js`), tags `wcag2a wcag2aa wcag21aa wcag22aa best-practice`, every route × 390/768/1440 + key states (menu open, walk mode, lens, museum approach, mini-player, 2-D grid dialog); plus a **keyboard walk**: Tab through each route recording focus order and asserting a visible focus ring (screenshot at each stop on `/`, `/map`, a chapter, `/paintings`), Escape/Enter/Space semantics on menu, cards, museum, dialog; `prefers-reduced-motion` and 200% zoom (1440 @ `deviceScaleFactor 2` + CSS zoom) sweeps. Output `docs/v7/qa/a11y.{json,md}`; gate = zero serious/critical.
3. `scripts/frames.mjs` — curtain frame capture (X1 acceptance): CDP screencast or `recordVideo` of card→chapter and Continue→next at 390/1440 with 4× CPU throttle; extracts frames to `docs/v7/qa/frames/` and asserts no uncovered page-B frame before the hold.
4. `scripts/walk-check.mjs` — the map/walk behaviour tests (M2/M4/M5/M9 acceptance) against a `window.__troyMap` debug hook (`map`, `slider()`, `state {focused, walk, activeIdx, dragging, leaving}`): overview label-fit at all 8 viewports, drag-pauses-walk with no snap-back, Continue/Stop, swipe-follows-map at 390 (`hasTouch`), card title line counts, curtain quiet (`map.on('move')` spy after a card tap), console clean.
5. `scripts/museum-check.mjs` — the museum step tests (A10 implementation order) against `window.__museum`.
Both hooks ship in production (static site, harmless) or gate on `?debug`.

**Audit deliverable — `docs/v7/AUDIT.md`:** every finding as `V7-nnn · route · viewport(s) · category (a11y | ux | design | perf) · severity P0–P3 · evidence path · proposed fix · Part-A item (or NEW)`. Findings that map to Part A cite the item; new ones get their own IDs and are fixed in the phase that owns the file. Also a **UX heuristics walk** written by hand (fresh eyes, all breakpoints): first visit, QR arrival on each chapter, the walk, the lens, the museum, menu, footer, back-button/deep-link behaviour, 320px, landscape phones, zoom 200%, reduced motion, offline-ish (no Mapbox token) fallback, tap-target ≥ 24px (≥ 44px for primary controls), touch-only affordances, hover-only affordances (none allowed).

---

## PART C — EXECUTION PHASES (dependency order; each phase ends with its gate re-run + commit + push + RUN-STATE)

**Phase 0 — Bootstrap + baseline (S).** Reconcile: `git status`, `git log --oneline -10`; archive `docs/PLAN.md → docs/PLAN-v6.md` and `docs/RUN-STATE.md → docs/RUN-STATE-v6.md` (v3/v4 already archived that way); copy this plan verbatim to `docs/PLAN.md`; open `docs/RUN-STATE.md` (v7 format: CURRENT PHASE · CURRENT ITEM · **NEXT ACTION** · DONE table item→commit→evidence · DECISIONS · BLOCKED/NOTES). `npm ci` if needed; `astro dev --background`; wire `qa:rag`, `qa:a11y`, `qa:frames`, `qa:walk`, `qa:museum`, `build:favicon` npm scripts; add the `window.__troyMap` / `window.__museum` debug hooks; extend matrices; run the **baseline**: shots(9) + probe + states + census + contrast + perf(all routes) + rag + a11y + frames + arrival → `docs/v7/qa/baseline-*` (screenshots gitignored like v5/v6 — findings are the artefact). Update auto-memory `cnwm-project-state.md` (v7 started, this session's model, phase).

**Phase 1 — The three audits → `docs/v7/AUDIT.md` (M).** Instruments + the hand UX walk at all breakpoints; every Part-A item confirmed/expanded into concrete findings with evidence; severity ranked; a "fix in phase" column. Commit the audit before fixing anything.

**Phase 2 — Foundations (M) — cascades into everything, so first.** G1 rag utilities (`text.ts nbsp()`, `text-wrap` roles) · G2 `.lines` clip fix · G3 contrast tooling + token decisions (moral body colour, chip pattern) · G6 `--ui-inset` compliance (mini-player, museum) · G7 motion tokens · X1 curtain (inline head script, font preloads, will-change, page-B tween) · N1 X-spin · N2 scroll-hide verify · I1–I4 favicon set + head + build-og port · G-L3 trailing slash. Gate: frames.mjs clean; contrast exit 0; rag clip-probe zero; favicon URLs 200 locally.

**Phase 3 — Home + footer (S–M).** H1–H6 · F1. Gate: shots 9 viewports; rag; contrast; Lighthouse home ≥ 98.

**Phase 4 — Chapter template, all five (L).** C1 drop cap · C2 moral contrast + parallax · C3 study centred · C4 interlude chip · C5 Where-to-next declutter + mini-player collapse · C6 rhythm · C7 hero mobile focus + portrait-video fix + scroll-hide check · C8 barbershop · C9 Ch2 reorder · C10–C12 · G5 em-dash sweep (JSON + UI, CONTENT-STATUS ledger). Gate: census rhythm ladder identical ×5; rag zero; contrast zero; a11y zero serious; audio verified on all six players (Playwright: click the island's real Play after scrolling it into view — `client:visible` gotcha); arrival.mjs unchanged or better.

**Phase 5 — Map + walk + lens (M–L).** M1–M13 · L1–L4. Gate: states.mjs (new states) zero collisions at 360/390/430/768/1024/1440/1920; projected-marker check for M2; Playwright drag tests (M4/M9); 60fps trace (M5); Lighthouse `/map` no regression (currently perf ~56 is a documented exception; do not make it worse — if the pitch change costs LCP, prefer a lighter first paint).

**Phase 6 — People + About (S).** P1–P5 · A1–A4. Gate: shots; rag; contrast; a11y.

**Phase 7 — The Museum (L).** U1–U10 (U9 last, only if budget). Gate: museum states; 60fps traces at 390 (DPR 1.5) and 1440; perf `/paintings` ≥ 80/70; a11y 100; reduced-motion grid parity; VideoTexture toggles on tap/zoom on iOS Safari (user-gesture rule) and desktop; portrait work hangs at true aspect.

**Phase 8 — The Gate (M).** Regenerate ALL evidence on the final build (baseline instruments + new ones), then **two consecutive clean fresh-juror passes** (Part E) on the identical build with only P2/P3 fixes between; live end-to-end verify (GH Pages: every route, favicon URLs, a chapter walk-through on a real phone-sized viewport, museum on live, console clean); write `docs/v7/REVIEW-GUIDE.md` (before/afters per Part-A item, both juror sheets, residual P2/P3 list, **human queue**: Kathy courtesy note on punctuation edits, `quote.source`/`portal.hook` decision, Vercel dashboard deletion, handoff playbook status); update `docs/CONTENT-STATUS.md`, `docs/v4/MOTION.md`, `docs/DEVIATIONS.md`, `docs/HANDOVER.md` if instruments/scripts changed; final push; auto-memory updated (`cnwm-project-state.md`: v7 complete, what shipped, what's queued).

**Usage-pressure pivot:** if the primary model looks likely to run out before Phase 7, pull Phase 7 forward after Phase 2 (museum-first) so the heaviest creative lift lands on the strongest model; Phases 3–6 are instrument-gated and model-tolerant.

Item → phase index: G1–G7 → P2 (enforced through P8) · H → P3 · X → P2 · M/L → P5 · C → P4 · F → P3 · N → P2 · P → P6 · A → P6 · U → P7 · I → P2 · G-L → P2/P8.

---

## PART D — RUN CONTINUITY (context compaction · session death · usage cutoff · model switch)

Disk is truth; the conversation is disposable — the v6 mechanism, unchanged:
- **D1 Two truth files:** `<P>/docs/PLAN.md` (this plan, verbatim; edited mid-run only to log a Wil decision) and `<P>/docs/RUN-STATE.md` (live ledger; `NEXT ACTION` always written before any stop).
- **D2 Atomic act:** one work item = implement → re-shoot the relevant viewports → commit → update RUN-STATE (→ push every ≤ 3 commits). Never park meaningful work uncommitted; never update RUN-STATE without the commit or vice versa.
- **D3 Recovery (any fresh session, any model):** read PLAN.md → RUN-STATE.md → resume at NEXT ACTION; `git status` + `git log --oneline -10`; dirty tree = suspect (build + re-shoot before committing; never discard silently); RUN-STATE vs git disagree → git + pixels win, fix RUN-STATE first; after a model switch re-run the current phase's gate instruments once; jurors always run on the strongest model available.
- **D4 End of every session:** update auto-memory `cnwm-project-state.md` (run status, phase, next action, session/model) so the memory layer survives too.
- **D5 Model/effort recommendation:** Fable 5, effort HIGH for Phases 0–6, XHIGH for Phase 7 (Museum) and every Phase 8 juror pass; fallback Opus 5 at MAX effort with the identical protocol (expect more fix-loop iterations at the gate — the gate, not the model, enforces the bar).

---

## PART E — FINISH CONDITION (the gate) + human queue

**Sheet A (Awwwards axes, unchanged from v6):** design · usability · creativity · content ≥ 8 at every breakpoint class (phone / tablet / desktop), zero P0/P1 defects; juror also names the ONE moment they'd retell (the Museum should still be it).
**Sheet B (Wil's ledger):** every Part-A item verified by the juror as **done as specified at every breakpoint class**, plus: axe zero serious/critical on every route/state; `contrast.mjs` (incl. pixel mode) exit 0; `rag.mjs` zero runts / zero clips; `states.mjs` zero collisions; `census.mjs` one rhythm ladder on the five chapters; `frames.mjs` clean; perf home ≥ 95, chapters ≥ 95, `/people` `/about` ≥ 95, `/paintings` ≥ 80 desktop / ≥ 70 mobile, `/map` ≥ its baseline; a11y 100 everywhere; keyboard walk complete; reduced-motion parity; live URL verified.
**Two consecutive clean passes by different fresh jurors on the identical build.** Then REVIEW-GUIDE + memory + push.

**Human queue (never blocks the run):** Kathy — courtesy note listing punctuation-only edits + the `Uri Gilbert Home` subtitle normalization; Wil — favicon pick if the juror is split, `quote.source`/`portal.hook` render decision, arrow-tail proposal if one emerged, Vercel dashboard deletion (still on him), handoff playbook execution (museum GitHub → transfer → Mapbox → mappings → QR release → zip backup); Brian Tolle — plaque typo `ONCE HOUSE THE` → `HOUSED` before etching (unchanged, still open).

## Critical files
`<P>/src/pages/{index,[chapter],map,people,paintings,about,404}.astro` · `<P>/src/components/{TroyMap,Museum,AudioStory,EmbedMap}.tsx` · `<P>/src/components/{Menu,SiteFooter,ChapterSpine,Lines,Icon,Button}.astro` + `icons.ts` · `<P>/src/layouts/Base.astro` · `<P>/src/lib/{curtain,text,url}.ts` · `<P>/src/styles/global.css` · `<P>/src/content/chapters/*.json` (punctuation + barbershop media order + Ch2 labels/address; **facts untouched**) · `<P>/src/content.config.ts` (heroFocus per orientation) · `<P>/src/data/{people,about}.ts` · `<P>/public/{favicon.svg,favicon.ico,favicon-*.png,apple-touch-icon.png,icon-*.png,site.webmanifest,og.png}` · `<P>/scripts/{shots,probe,states,census,contrast,perf,arrival}.mjs` + new `{rag,a11y,frames,build-favicon}.mjs` + `build-og.mjs` (ported) · `<P>/package.json` (devDeps: `opentype.js`, `png-to-ico`, `chrome-launcher` explicit) · `<P>/docs/{PLAN,RUN-STATE,CONTENT-STATUS,DEVIATIONS,HANDOVER}.md`, `docs/v4/MOTION.md`, new `docs/v7/{AUDIT,REVIEW-GUIDE}.md` + `docs/v7/qa/`.

## Verification (how the whole thing is proven)
Pixels over code: every item re-shot at its viewports and logged with evidence paths in RUN-STATE. Phase gates in Part C; the instrument set in Part B; the Part E gate ×2 on the final build; live verify on GH Pages. Playwright gotchas: AudioStory tests must click the island's real Play button after scrolling it into view (`client:visible`); Playwright scripts live in the repo root or `scripts/` (scratchpad can't resolve the package); Chromium computes IntersectionObserver against the target's OWN clip-path (never clip the observed element); CDP throttle is invisible to `navigator.connection` in headless (`arrival.mjs` shims it); Caslon's narrow "0" makes 54ch ≈ 65–70 real characters.

## Risks & cut-line
1. **Museum scope creep** (U3 finish work eats the run) — timebox; U9 stretch only; the 2-D grid remains the guaranteed fallback; ship the museum refinements only if the juror still names it the signature moment.
2. **Ch2 reorder regressions** (spine numerals, two audio islands, deep links `#scene-1`) — Playwright walk of both players + spine order test before commit.
3. **Curtain rewrite** touches every navigation — keep the fail-open timers; test menu, cards, Continue, footer, People, Paintings, 404 links; `pageshow` bfcache path.
4. **Map perf** already the weakest (LH ~56) — pitch/zoom must not push LCP; measure before/after.
5. **Punctuation edits in locked prose** — punctuation ONLY; the CONTENT-STATUS ledger + Kathy note is the safety net; audio timings never re-run.
6. **Favicon at 16px** — a monogram can smear; the 3-candidate juror pick and the 1×/8× inspection are the guard.
Cut-line order if the run must shrink: U9 stairs → footer decorative wordmark → moral parallax (keep contrast fix) → museum finish detail (keep frames + aspect + 360 + inspect layout).

---

## KICKOFF PROMPT (paste into the fresh execution session — idempotent: bootstraps, resumes, survives model switches)

```
You are running CNWM v7 — "The Last Ten Percent" — the QA session that takes the Charles Nalle Walking Memorial website from ~90% to 100%, fully autonomously, to completion.
Repo (local): /Users/thebayniac/Documents/(A) Documents/(A) WBM Enterprises/(B) Notable/(B) Clients/Charles Nalle/cnwm-v2  (ALWAYS quote the path — parentheses/spaces).
Repo (remote): https://github.com/makeitnotable/Charles-Nalle-Walking-Memorial — local branch main tracks origin/v2 (the deploy branch); plain `git push` only — NEVER push main:main (main / match-figma-designs / feat/* / fix/* are the legacy SPA; never touch them).
Live site: https://makeitnotable.github.io/Charles-Nalle-Walking-Memorial/ — GitHub Pages, auto-deployed by .github/workflows/deploy.yml on every push to v2 (verify with `curl -sI` + the Actions run after each push; live must equal HEAD before any juror pass).

THE PLAN (the constitution): /Users/thebayniac/.claude/plans/users-thebayniac-documents-a-documents-zesty-giraffe.md
Wil's source review (only for lookups; the plan already enumerates every request): "/Users/thebayniac/Documents/(A) Documents/(A) WBM Enterprises/(B) Notable/(B) Clients/Charles Nalle/Meeting 2026_08_15 15_48 MDT - Notes by Gemini.md" — Notes lines 1–113, transcript lines 114–571 (`sed -n`), base64 screenshots lines 572–600.

FIRST ACTIONS, in order:
1. Read the plan file above fully. Then: if <repo>/docs/PLAN.md is not this v7 plan, archive the current docs/PLAN.md → docs/PLAN-v6.md and docs/RUN-STATE.md → docs/RUN-STATE-v6.md (git mv), copy the plan file verbatim to docs/PLAN.md, and commit ("v7: plan bootstrapped").
2. Read <repo>/docs/RUN-STATE.md. If it has a v7 section → resume exactly from its NEXT ACTION. If not, this is bootstrap: create it in the Part D1 format and begin Phase 0.
3. Run `git status` and `git log --oneline -10`. Dirty tree = suspect: build and re-shoot before committing anything; never discard work silently. If RUN-STATE and git disagree, git + pixels win — correct RUN-STATE first.
4. Update auto-memory cnwm-project-state.md: v7 run started/resumed, this session's model, current phase, next action.

OPERATING RULES (from the plan, non-negotiable):
- Disk is truth; the conversation is disposable. One work item = implement → re-shoot the relevant viewports → commit → update RUN-STATE as ONE atomic act; push every ≤3 commits; always write NEXT ACTION before stopping for any reason.
- Identify before you fix: Phase 1 produces docs/v7/AUDIT.md (instruments + a hand UX walk, all breakpoints) and is committed before any fix lands. Then fix in Part C order; every fix is verified by pixels at phone/tablet/desktop.
- Fully autonomous — no check-ins. Human-only items go to the REVIEW-GUIDE queue, never block.
- Content: Kathy's facts are untouchable; em dashes are removed everywhere but ONLY by punctuation substitution inside locked prose (zero word changes; log every one in docs/CONTENT-STATUS.md; audio timings are never re-run). UI/design copy per the plan's locked decisions and "decisions I made for you".
- Guardrails: no scroll-jacking, reduced-motion parity everywhere, keyboard reaches everything, no hover-only affordances, no loading gates, floating UI on --ui-inset, motion on the house tokens except documented exceptions, zero console errors, withBase() for every URL.
- Finish = Part E gate: Sheet A (Awwwards axes ≥8 every breakpoint class, zero P0/P1) + Sheet B (every Part-A item done as specified at phone/tablet/desktop; axe zero serious/critical; contrast/rag/states/census/frames clean; perf bars; a11y 100), TWO consecutive clean passes by fresh jurors on the identical build; live end-to-end verify; docs/v7/REVIEW-GUIDE.md written; memory + RUN-STATE updated; pushed.
- Usage pressure: Part C pivot (museum-first after Phase 2); atomic act at cutoff; this same prompt resumes in the next session on any model.
Recommended model/effort: Fable 5 — HIGH for Phases 0–6, XHIGH for Phase 7 (Museum) and all Phase 8 juror passes; fallback Opus 5 MAX.
```
