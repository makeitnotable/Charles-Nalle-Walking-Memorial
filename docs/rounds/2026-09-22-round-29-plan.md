# Client round 29 — the 1858 lens, see-through again

*Wil, 2026-09-22, on round 27's opaque fill: "Should have a bit of
transparency to it so you can see the map behind it just like before." Base
`df12705` (= the `v2` tip: another session's round 28, `/paintings`' drawer,
landed while this round ran, so this one was rebased onto it and numbered 29),
tagged locally `client-round-29-base`. Revert: `git reset --hard
client-round-29-base`.*

## What ships

| # | Change | File |
|---|---|---|
| 1 | The lens shell's colour is the original 70% black wash again; round 27's geometry stays, so the wash covers the canvas's box, runways included, and the map behind the bars is dimmed exactly like the map around the plate | `global.css` |
| 2 | The shell declares `#101010` as its edge colour — 70% black over the map's ground `#353535` — so `<body>`, which Safari reads for its bar tint and which the sampler cannot read through a translucent element, matches the dimmed map while the lens is open, and returns to the map's grey on close | `TroyMap.tsx` |
| 3 | The Mapbox logo and (i) no longer fade under the lens: the map is visible, so its attribution is owed, and they sit dimmed under the wash as they did before round 27 | `global.css`, `TroyMap.tsx` |
| — | `map-framing.mjs`'s lens probe follows: wash `rgba(0,0,0,0.7)`, `<body>` `rgb(16,16,16)`, controls at opacity 1, every edge pixel of a capture over the flat stub map ≈ 16,16,16 | instrument |

## Verification (measured 2026-09-22, Chromium 141 / Playwright 1.62)

- `npm run build` + check-css ✓ · `npm run check` 0 errors 0 warnings ✓ ·
  `npm run qa:scope` ✓.
- `map-framing`, T=0: cameras and the walk view unchanged (14.9 / 14.6 /
  15.1 / 14.62 / 15.01; door 16 / 16 / 16 / 52 / 68); 19 checks per viewport ✓.
- `map-framing`, T=110 / E=129, the lens probe at every viewport: shell rect
  = canvas rect; wash `rgba(0, 0, 0, 0.7)` at opacity 1; the plate inside the
  visible layer; controls at opacity 1; `<body>` `rgb(16, 16, 16)`; all six
  edge pixels of a capture over the flat stub map `16,16,16`; after Back to
  today `<body>` back to `rgb(53, 53, 53)`. 26 checks per phone viewport, 22
  on tablet and desktop, all ✓.
- `npm run qa:snap` — see the round report: no route may drift.

## Only the phone can show

Whether Safari's bar tint at `#101010` reads as one with the dimmed map, and
how the change animates on open and close.

## Closed

Approved on his phone, 2026-09-22: "Looks perfect."
