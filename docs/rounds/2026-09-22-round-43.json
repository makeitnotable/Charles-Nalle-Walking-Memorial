{
  "round": 43,
  "label": "the chapter pages — the runway, behind ?scroll=sync for Wil's phone (his choice: 'Fine! Let's try it, if I don't like it we'll officially go with A'): the document scrolls a solid runway so both bars collapse and return, the chapter rides in a fixed, clipped <main> the compositor moves, the top bar keeps its solid section colour, the toolbar returns over a solid fill; the default (the still document) untouched",
  "date": "2026-09-22",
  "plan": "docs/rounds/2026-09-22-round-43-plan.md",
  "base": {
    "tag": "client-round-43-base",
    "commit": "833e382615734339193637c305bb92ae48b203df",
    "branch": "v2"
  },
  "allowed": [
    "src/layouts/Base.astro",
    "src/styles/global.css",
    "src/pages/[chapter].astro",
    "src/components/AudioStory.tsx",
    "scripts/chapter-still.mjs",
    "docs/RUN-STATE.md",
    "docs/v4/DECISIONS.md",
    "docs/PLAYBOOK-MOBILE-CHROME.md",
    "docs/rounds/2026-09-22-round-43.json",
    "docs/rounds/2026-09-22-round-43-plan.md"
  ],
  "snap": {
    "allowedRoutes": []
  },
  "notes": "Wil, 2026-09-22, after round 42 (A, the approved pages restored): 'Can we have everything stay the same but will it work where the bottom toolbar will disappear on scroll down … I don't mind if the bottom toolbar has to have a solid fill … the same way on the address bar.' Told the one thing no page can remove — Safari moves the top bar in the same animation, so it shrinks and grows with the toolbar while its fill can stay the solid section colour — and that the runway's toolbar fill was the page brown on his phone in passes 15–16, he chose: 'Fine! Let's try it if I don't like it we'll officially go with A.' Device-first: behind ?scroll=sync, the default byte-identical in behaviour. The mode is round 23's pass-13 runway, re-applied from its own diff and adapted: the flag regex takes `sync` (the default branch still assigns `inner`); #reader wraps <main>'s content (an unstyled block off the flag; the sampler's ground walk counts its children as <main>'s); under the flag <main> is fixed and clipped at 100lvh, #reader is translated by −scrollY on every scroll event and re-read for 600 ms after the last, <body> is sized to the chapter and carries the section's colour from the sampler (the top bar's solid fill, and Safari's fill for the returning toolbar), fragments are landed by scrolling the document; the walk rail is moved out of #reader at runtime by the sync script (pass 15 found a fixed box inside the moving reader riding away — done at runtime so the default's DOM is untouched, not through a slot); the mini player is portaled to <body> only in this mode (pass 13 did it in every mode); the corner menu rides the same top inset as the rail (round 41's rule, this mode only); the cover and visor are `doc`-only (pass 13). Pass 15's colour map and pass 16's transparent <main> are not brought back: the phone showed Safari painting the returning toolbar as an opaque fill whatever the runway held, and that fill is now what he accepts. qa:still gains the flag's checks (the mode, the geometry, the runway's colour following the section, the rail out of the reader, the portal, the deep link, the menu on the inset, the default's #reader untransformed). Off the flag: one unstyled wrapper div in the DOM and nothing else; no route may drift."
}
