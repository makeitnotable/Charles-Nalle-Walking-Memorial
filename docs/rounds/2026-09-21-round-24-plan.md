# Client round 24 — `/paintings` with fresh eyes: the hall through Safari's bars

*Wil's two `/paintings` screenshots of 2026-09-21 (12:17), one page only.
Base `1e5b07d` (= the `v2` tip and the live site after round 23's closeout),
tagged locally `client-round-24-base`. Revert: `git reset --hard
client-round-24-base`. His answers to the diagnosis report's four questions,
verbatim: (1) "I want the bars to collapse while walking"; (2) the gradient
or fade "is the back up option, we will come back to this if the first idea
requested can not be achieved"; (3) "This request is paintings page only";
(4) the at-rest top bar stays — "Correct". And the framing: "a completely
separate page with a completely separate request and set of requirements
that should affect and only apply to this page".*

## 1 · What the screenshots show, and why

**His words.** Screenshot 1, before scrolling: "make the area behind and
around the bottom toolbar transparent so it behaves like normal Liquid Glass.
The goal is that users can see more (ideally all) of the painting hall
through/around the toolbar." Screenshot 2, scrolled into the hall with the
bars minimized: the same for "the area behind and around the top address
toolbar". Both: "If full transparency isn't the cleanest implementation, an
acceptable alternative is a subtle gradient or fade-in background fill,
similar to what we're using on the Map page."

**Diagnosis.** One defect, one cause. The corridor stage is `position:
sticky`, one viewport tall, with the WebGL canvas absolutely positioned
inside it (`src/components/Museum.tsx`). Safari 26 clips a pinned box's paint
at the layout viewport, stuck or not — playbook §1 (a fixed bleed of 160px
rendered 21), round 8 (the stage's box reaches ~200px past the toolbar band
at rest and his screenshot shows the band cut at the viewport's edge to the
pixel) and round 23's device pass 5 (a stuck sticky box "now it's
transparent"). Only in-flow page paint reaches the bar regions. What Safari
paints there instead is what lies behind the stage in the document: the page
ground on `<main>` under the toolbar, and above the viewport the slot's empty
region that has scrolled past, under Safari's own tint from `<body>`. All of
it is `#1d1411`, so both regions read as a flat slab with a hard cut.

The map never had this because its shell is ordinary in-flow content, grown
by E below and T above the visible area with the page landed at scroll T.
The "subtle gradient" he sees there is Safari's glass tint over live map
pixels; nothing in the site draws a gradient behind the bars.

**What was tried before.** Round 8 measured the bottom band and closed it as
a platform limit (he accepted it on 9/17); un-sticking the corridor was ruled
out as the page's scroll mechanic. Round 18 built a bottom bleed of the
canvas behind `?glass=1`, never checked on the device, which could not have
painted through the stage. Round 23 solved the chapters with the still
document — bars that never collapse — which his answer (1) rules out here.

## 2 · What ships — the runway strips, behind `?glass=1`

The stage keeps everything: its box (`stage.clientHeight` is the composition
basis — FOV, thresholds, the walk's scroll total, the sheet split), its
scroll mechanic, its `overflow: hidden`. Three additions, all inert where
there are no bars:

1. **The bleed.** The canvas renders B rows above and below the stage,
   B = `round(down, min(110px, 100lvh − 100svh), 1px)` — the map's T
   expression, 110 on his phone (bars 107 / 92), 0 wherever lvh == svh.
   `camera.setViewOffset(w, h, 0, −B, w, h + 2B)` keeps the stage's own band
   framed pixel-for-pixel (measured, §3). The stage clips it; nothing shows
   through the stage.
2. **The strips.** Two in-flow `<canvas>` elements in the slot, siblings
   placed BEFORE the stage so its opaque ground and canvas paint over them.
   Every frame, after the render and in the same task (the drawing buffer is
   not preserved past it), the bleed rows are copied into them and they are
   placed by transform:

   | strip | shown while | placed at (viewport px) | rows |
   |---|---|---|---|
   | top | the stage's top edge is at or above the viewport's (stuck, or scrolling out at the end) | −(48 + B) → +48 | 48 padding + B bleed + 48 overlap |
   | bottom | the stage's box reaches svh, and the strip stays inside the slot | svh − 48 → svh + B + 48 | 48 overlap + B bleed + 48 padding |

   svh is the bars-expanded viewport height and holds still (playbook §1);
   the toolbar band exists only in that state. With the bars minimized the
   bottom strip sits behind the stage and below the screen, harmless.
3. **The lag answer.** A strip is page paint moved by the main thread, so
   playbook §3's frame of lag applies. Rather than a visor, geometry: the
   48px overlap on the viewport side is hidden behind the opaque stage and
   the 48px padding past the bar region is the nearest rendered row
   stretched, so a lag in either direction moves the seam inside the overlap
   or the padding and never opens a slit of page ground. A one-frame velocity
   lead (the round-23 estimator, symmetric) centres it. What remains is a
   texture phase of a few pixels in a blurred, dark band — only his phone
   can judge it. The top strip fades in over the first 100px after the stage
   sticks, so the ceiling arrives over the header's tail as a fade, not a
   cut. A paused render loop hides both strips.

**Flags, one page load each, inline in the URL:**

| flag | what it does |
|---|---|
| `?glass=1` | the runway, as above (the default, script-placed) |
| `?glass=1&runway=track` | the strips placed by a scroll-driven animation on the compositor, self-checked at rest only (round 23's open measurement: whether this Safari accelerates `animation-timeline: scroll()`) |
| `?glass=1&runway=off` | the bleed renders, no strip exists — the cost of the taller render target alone |
| `&debug=1` | a readout: driving path, timeline support, B, svh, innerHeight, the stage's and strips' edges, frame interval, velocity and lead |

Off the flag every route is byte-identical (§3). When he approves on the
device, it comes off the flag as the map's T did: drop `[data-glass]` from
the `--museum-b` selector in `global.css` and the `dataset.glass` test in
`museumB()`.

Files: `src/components/Museum.tsx`, `src/styles/global.css`,
`scripts/museum-runway.mjs` (+ `package.json` scripts: `qa:runway`), and the
docs in the manifest. `Base.astro` is untouched: `?glass=1` and `?debug=1`
were already parsed there for rounds 17–23.

## 3 · Verification (measured 2026-09-21, Chromium 141.0.7390.37 / Playwright 1.62.1)

- `astro check`: 0 errors, 0 warnings. Build + check-css clean.
- `qa:scope`: every changed path in the manifest; `package.json` held to
  `scripts`.
- `qa:snap` on the UNTOUCHED base build first: 36/36, 0 drift against the
  committed baselines — this container's Chromium matches them, so the gate
  is live. Then on the new build: **36/36, 0 drift, 0 failed.** B is 0 in
  Chromium and the strips are never created, so no route may move, and none
  did.
- `qa:runway` (`scripts/museum-runway.mjs`), B stood in at 110 through an
  init-script `<style>` with `!important` (the technique of
  `map-framing.mjs`), at 390×645 (the iOS layout viewport with the bars
  expanded) and 430×720, sessions OFF / ON / BLEED / TRACK, positions rest /
  stuck (40% of the slot) / past the hall / just stuck / +150 / back at
  scroll 0, plus 1440×900 with the flag and no bars: **161 checks, 0
  failed.** Among them:
  - the stage box identical to OFF at every position; the canvas
    `stage + 2B` tall starting B above it (295.6 → 185.6 / 865 at rest);
  - the canonical projection of a work (the live camera posed at one rail
    pose) and `--cnwm-chip-y` identical with and without B — the framing
    invariant; the live projection under the walk within 1.5px between
    sessions (the dolly's last millimetres of settle);
  - the bottom strip at rest at 597 → 803 (svh 645), the top strip hidden;
    stuck, the top strip at −158 → 48 and the bottom at 597 → 803; past the
    hall, both hidden; just stuck, the top strip at opacity 0, fully in
    150px later; back at scroll 0, the bottom strip only;
  - every visible strip's sampled rows equal the same frame's canvas rows
    (mean abs diff 0–0.7 of 255) and are not a fill (luminance σ 3–9);
  - TRACK: the path reports `track`, the self-check never demotes it, the
    stuck position lands to the pixel — so Chromium places a scroll-timeline
    strip correctly at rest; whether his Safari does, and on the compositor,
    is the device measurement;
  - the desktop guarantee: 1440×900 with `?glass=1` — no strip, canvas equal
    to the stage, `runway.b` 0;
  - zero page or console errors in every session.
- **Cost, in this container's GL (not the phone's).** A scripted walk, 120
  frames from rail 0.1 to 0.9, one scroll per animation frame:

  | viewport | OFF median / p95 | bleed only | ON (strips) |
  |---|---|---|---|
  | 390×645 | 25.0 / 28.6 ms | 30.1 / 34.7 | 36.0 / 41.1 |
  | 430×720 | 29.3 / 34.1 ms | 35.1 / 41.6 | 41.2 / 47.4 |

  Roughly half the added cost is the 2B extra rows of render target, half
  the two copies. The phone's GPU is not this one; the number to watch is
  his walk's smoothness with and without the flag. Levers if it drops
  frames: copy the bottom strip only while the bars are expanded, halve the
  copy rate, or trim B toward the measured bars (107 / 92).

**What only the phone can show:** the bars themselves; whether Safari
composites the strips into both bar regions (the map's runway says yes for
in-flow paint); the seam in motion — a frame of lag against the overlap; the
frame rate; and the `?runway=track` question.

## 4 · Device pass — what to look at, and what a FAIL looks like

Live URL + `/paintings?glass=1` (and the variants). Same phone, Top layout.

1. **At rest, page top.** The floor should continue under the bottom
   toolbar's glass, as the map does. FAIL: the flat brown band of screenshot
   1; or a hairline / step at the toolbar's top edge where the strip meets
   the stage.
2. **Scroll into the hall (the bars minimize).** The ceiling should continue
   behind the status bar and the pill. FAIL: flat brown; a slit of brown
   under the pill while scrolling; a visible tear in the ceiling at the
   viewport's top edge. Look once slowly and once with a fast flick, both
   directions.
3. **Scroll up a little mid-walk (the bars re-expand).** Both bars should
   show the hall — ceiling above, floor below.
4. **Frame rate.** Walk the hall with the flag and without. A FAIL is a
   walk that stutters with the flag and not without; say which viewport
   state (bars minimized or expanded).
5. **Tap a painting** with the bars minimized: the wall continues above the
   drawer's composition. Back to the hall, Skip, the stills below: nothing
   of the strips should ever be visible INSIDE the page.
6. **`?glass=1&runway=track`** — 1 to 3 again. If the seam is cleaner in
   motion than the default, that is the answer round 23 could not get; one
   screenshot of `?glass=1&runway=track&debug=1` mid-flick settles whether
   the path reads `track (compositor)` or has demoted itself.
7. **Only if 4 fails:** `?glass=1&runway=off` — the bleed alone. If that
   walks smoothly and the strips do not, the copies are the cost.

## 5 · If the first idea fails on the device

His fallback, in order of what stays possible: the bottom band at rest can
carry a static copy of the floor (an in-flow strip placed once — nothing
moves at rest, so no lag) or a fade from the floor's colour into the page
ground; the top region while walking can only carry a static band (a
gradient in the ceiling's colour, scrolling with the document — no lag, but
not the live hall). Not built this round: he asked for the live hall first.

## Revert

`git reset --hard client-round-24-base` (= `1e5b07d`) and push `v2`; or
`docs/v4/DECISIONS.md` round 24's file-level revert.
