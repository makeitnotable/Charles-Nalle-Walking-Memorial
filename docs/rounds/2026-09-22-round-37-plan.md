# Client round 37 — round 36 signed off and `/paintings` shipped again (docs only)

*Wil, 2026-09-22, on round 36 live (Skip holds still while the reader pans
the hall): "As far as i can see paintings page is done push to master and
live site and document everything and the final version that we are
shipping to the client accordingly and to best practices." Base `5e068cf`
(= the `v2` tip at the push, round 36's own commit), tagged locally
`client-round-37-base` and pushed as `claude/client-round-37-base`. Docs
only — no source file changes, so the visual gate is not run; `qa:scope`
against this round's manifest.*

## 1 · What this round is

Round 36 changed the site (`docs/rounds/2026-09-22-round-36-plan.md`); this
round records that Wil signed it off on his phone and ships it the way round
32 established and rounds 34 and 35 repeated:

- **The handover** (`docs/HANDOVER.md`, version 1.3): the Paintings entry in
  the changelog gains round 36 in the museum's language, and the page's
  instruments now include `qa:skip`.
- **The ledger** (`docs/RUN-STATE.md`), **the decisions log** (the round-36
  entry closed; the "Ship" entry's refresh paragraph for this round), **the
  playbook** (§1 and §10: the corner-control shift is closed for both top
  corners, on his phone), **the entry-point note** (`docs/NEXT-SESSION.md`)
  and **the round-36 plan** (its checklist and Closed section).
- **The mirror.** `main` is refreshed with a merge commit whose tree is this
  `v2` tip's exactly (`git commit-tree <tip>^{tree} -p <main-tip> -p <tip>`),
  pushed as a fast-forward — never a force push — and `release/2026-09-22` is
  moved to the same tip. The mirror commit's own message names both parents.

## 2 · The final version, as shipped

The `v2` tip this round's commit makes, carried by `main` and marked by
`release/2026-09-22`. It holds everything signed off on 2026-09-22:

- `/paintings` — rounds 24–31, signed off after round 31 ("paintings page is
  done"; the ship round 32), and **round 36**, signed off on the live site
  with the same words after his device pass: the hall's Skip button reads its
  own inset (the gutter, the top and left safe areas) and no longer moves
  when Safari's bars collapse under a pan; `npm run qa:skip` measures it.
- `/map` — rounds 17–22, 27, 29–30, signed off after round 29 ("map page is
  done"); the record round 34.
- The bottom lane under Android's gesture bar — round 33, signed off after
  its live push ("these edits are done"); the record round 35.
- The chapter pages as of round 23's latest device pass in the tree (pass
  17; another session; its device read open).

What the remote held when this round was written: `main` at `d3d827d` (the
round-35 mirror, whose tree is `e606214`'s) and `release/2026-09-22` at
`e606214`. The mirror commit's message names what `main` held at the push.
Where another session's push lands first, the mirror is rebuilt on the new
`main` tip — the fast-forward rule makes the order safe.

## 3 · Verification

Recorded in the commit and the report: `qa:scope` clean against the round-37
manifest; the `main` tip's tree equal to the `v2` tip's (`git rev-parse
main^{tree} v2^{tree}`); `release/2026-09-22` at the `v2` tip; the Actions
runs. What the runs did on the first push: the `v2` push's run (290) was
cancelled by the `main` push's run (291) under the workflow's
cancel-in-progress concurrency, and 291 published the tree (success, 18:45
UTC) — the same tree either way, so the live site is the shipped one. This
second push, which records that, waits for the `v2` run to finish before
`main` is mirrored again, so both runs complete. The site itself is unchanged
by this round (docs only) — the live build is round 36's, verified in its own
plan and on his phone.

## Revert

The docs: `git reset --hard client-round-37-base` (= `5e068cf`). The mirror:
`docs/v4/DECISIONS.md`, "Ship" — restoring the retired app on `main` is a
force push that publishes it, so only with the workflow disabled or with `v2`
pushed again right after; moving `release/2026-09-22` back is a force push
of the branch.
