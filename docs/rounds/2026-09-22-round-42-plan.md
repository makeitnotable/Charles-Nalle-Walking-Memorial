# Client round 42 — A: the working version he approved, and nothing else

*Wil, 2026-09-22, on round 41's link: "Nothing was changed or fixed. Go
back to the working version I approved." A, as agreed before B was tried
("If i do not like B, be prepared to go with A"). Base `566dc62` (= the
`v2` tip, round 41), tagged locally `client-round-42-base`; the SHA is
pinned in the manifest. Revert: `git reset --hard client-round-42-base`.*

## 1 · What ships

**Round 38, exactly.** The source changes of rounds 40 and 41 are
reverse-applied in one step (`git diff 3d9aa1c HEAD -- src`, reversed) and
`scripts/chapter-still.mjs` is restored from round 38, so `src/` and the
instrument are byte-identical to `3d9aa1c` — round 38 live, the version he
approved on his phone ("Works great!"). Gone: the `edge` value of the
`?scroll=` flag and the `&band=` flag, the `.edge-trigger` element and its
rules, the round-41 menu rule, the instrument's edge and inset sessions.
Back: nothing — the still document was the default throughout; both flag
rounds were byte-identical off the flag (`qa:snap` 36/36 at 0 drift each
time), so no visitor ever saw them without the flag. This round changes
nothing on screen; it removes the code and closes the thread.

**What closes with it.** The bottom toolbar hides only when the document
scrolls, and Safari moves the top bar in the same animation; the static
top fill he approved exists only while the document is still. Both flagged
attempts to have the toolbar hide (rounds 40–41) were rejected on his
phone. The fixed bars are the decision (DECISIONS.md, round 23, "Round 42").

**What he named, and is not done here.** "There is one other that I liked
that was the working version with one change, the bottom toolbar only had
a solid fill when it appeared on the user scrolling up the page." That is
round 23's runway — device passes 13–16, commit `042518b`: the document
scrolls a solid runway (both bars collapse and return), the chapter rides
in a fixed, clipped `<main>` the compositor moves, and the toolbar's region
shows the runway's colour — a solid fill in the section's colour when the
toolbar returns, which at the time was the defect being chased and is now
the thing he liked. It is in the history and can come back behind a flag
in one pass if he asks; per rule 4 it would go to his phone first.

## 2 · Verification (Chromium 141 / Playwright 1.62)

- The revert, proved: `git diff 3d9aa1c -- src scripts/chapter-still.mjs`
  is empty; the built `/bakery` carries no `edge-trigger` and the flag
  regex is `(inner|doc)`.
- `npm run build` + `check-css` (6 guards) ✓ · `npm run check` 0 errors, 0 warnings ·
  `npm run qa:scope` — every changed path in the round-42 manifest ✓.
- **`npm run qa:still`: 187 checks, 0 failed** (round 38's instrument, round 38's tree).
- **`npm run qa:snap`: 36 shots, 36 ok, 0 drift, 0 failed (205.1 s).** No route may drift.

## 3 · Checklist

- [x] Revert point: `client-round-42-base` = `566dc62` (local tag; the SHA in
      the manifest, and `v2` holds the commit).
- [x] Scope gate: `qa:scope` clean against the round-42 manifest.
- [x] Visual gate: `qa:snap` 36/36 at 0 drift, no route allowed to drift.
- [x] The instrument: `qa:still` 187 checks, 0 failed (round 38's count exactly).
- [x] Device first: nothing new for the phone — this is the tree he approved.

**Verified here:** everything in §2. **Not verifiable here:** nothing new;
the behaviour is round 38's, approved on his phone today.

## Revert

`git reset --hard client-round-42-base` (= `566dc62`), then push `v2` —
that brings round 41's flag code back (inert off the flag).
