# Client round 5 — Charles Nalle Walking Memorial (v2)

## Context

Wil reviewed round 4 live (`20e8680`, Actions run 224) and filed three edits,
two of them reversing round-4 calls. Base: `20e8680` = the `v2` tip, tagged
locally `client-round-5-base`. The edits are small and touch the same two
files, so the conductor applied them directly with the same gates (build,
`astro check`, hall-stress, scope, snap) rather than fanning out.

## Item-by-item decisions

### 1 · Buttons back to ALL CAPS at the original weight (every page, every breakpoint)
`.btn, .btn-sm`: `text-transform: uppercase` restored; `font-weight: 400`
stands (the 700 of round 3 stays reverted — "I do not like the heavier
weight"). Every label round 4 re-authored in Title Case goes back to its
original sentence case (rendered in caps by the rule, as before): the four
label-only pages (`404`, `about`, `people`, `styleguide`) are restored from the
round-4 base; the labels in `TroyMap.tsx`, `Museum.tsx`, `[chapter].astro` and
`index.astro` are edited back by hand because those files carry other round-4
hunks. Tracking (0.08em) untouched.

### 2 · Face Forward ↔ dots (every breakpoint)
The bottom-centre column's wrapper gap 16 → **24px** (`gap-6`, `--sp-3`).
The dots ↔ counter gap on the `<nav>` stays 16. hall-stress's "Face forward
above the dots" assertion follows (24).

### 3 · Drawer stroke colour (phones + portrait tablets)
The divider between the painting title and the artist study is
`.museum-study { border-t border-primary-7/60 }` (Museum.tsx `StudyNote`),
which Tailwind emits as `color-mix(in oklab, var(--color-primary-7) 60%,
transparent)` with `#80412b99` as the fallback. `.museum-sheet` now carries
those same two declarations, so the stroke and the divider are one colour on
every engine. Radius (32px), sides, open bottom, `- 1px` and `clientTop`
handling from round 4 all stand.

## Files that may change
See `docs/rounds/2026-09-16-round-5.json`.

## Revert
`git reset --hard client-round-5-base` (= `20e8680`, the round-4 `v2` tip).
