# Client round 45 — the chapter mini player centred on phones and tablets

*Wil, 2026-09-23: "Play / pause controls should be centered on tablet and
mobile (same position as the take the walk button on the map page) tapping
the play pause button or anywhere on the controls should allow the use to
pause or play the audio. … this is positioning task only." Base `cf37acc`
(= the `v2` tip, round 44, = `release/2026-09-22`), tagged locally
`client-round-45-base`; the SHA is pinned in the manifest.
Revert: `git reset --hard client-round-45-base`.*

## 1 · What this round is

One component, one instrument number.

- **The position.** The mini player — the pill that appears once narration
  plays and the main control has scrolled off screen — sits bottom-centre
  under 1280px (phones and tablets, the 20px and 40px gutter tiers), on the
  lane the map's "Take the walk" door uses: `--map-lane`, the bottom inset
  −4px on phones and +12px from 640px (global.css, round 20 / round 33).
  From 1280px (the 56px gutter tier, desktops) it keeps the bottom-left
  corner exactly as before; the corner menu owns the right.
- **The control.** The pill is the button. Before, only the orange circle
  toggled and the label and time beside it were inert; a button inside a
  button being invalid HTML, the circle is now a mark inside the pill and
  the pill carries the control's accessible name ("Play/Pause narration:
  …"). Hover on the pill lights the circle as before. The main control in
  the page is untouched.
- **Everything else as approved.** The runway portal (round 43), the shrink
  beside the Onward row on phones, the collapse to the time alone after the
  transcript, the fade while the main control is on screen, one narration
  at a time — unchanged.

## 2 · Verification

- `astro check`: 0 errors, 0 warnings. `npm run build` + check-css: clean.
- `qa:scope` against this manifest: clean.
- `qa:still`: the mini player's lane check moves from 20 to 16 (the map
  door's phone lane); every other check unchanged.
- `qa:snap`: no drift — the pill is never in a rest shot.
- Screenshots (Chromium, `astro preview`): `/bakery` at 390×844 (the runway
  and `?scroll=inner`), 768×1024 and 1440×900 with narration playing and the
  main control scrolled away; the pill centred on phone and tablet on the
  door's lane, bottom-left on desktop; tapping the pill's text pauses and
  resumes.

Not verifiable here: the pill against Safari's returning toolbar on Wil's
phone. Same lane as the map door he approved in round 20 and round 33.
