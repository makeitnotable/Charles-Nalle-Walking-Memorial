# Client round 35 — round 33 signed off and shipped (docs only)

*Wil, 2026-09-22, on round 33 live (the bottom lane under Android's gesture
bar): "As far as i can see these edits are done push to master and live site and document everything and the final version that we are shipping to the client accordingly and to best practices." Base `26fff0d` (= the `v2` tip at the push), tagged locally
`client-round-35-base`. Docs only — no source file changes, so the visual
gate is not run; `qa:scope` against this round's manifest. Numbered 35: round
34 (the map's sign-off) and round 23's device passes landed on `v2` from other
sessions running in parallel; this round was rebuilt on each new tip.*

## 1 · What this round is

Round 33 changed the site (`docs/rounds/2026-09-22-round-33-plan.md`); this
round records that Wil signed it off and ships it the way round 32
established and round 34 repeated:

- **The handover** (`docs/HANDOVER.md`, version 1.2): the changelog gains the
  Android entry in the museum's language, and §8's "proven on one phone" note
  becomes two phones — the iPhone for Safari's bars, the Pixel 6 for the
  margin above Android's gesture bar — with the phones and browsers that were
  NOT measured named as before.
- **The ledger** (`docs/RUN-STATE.md`), **the decisions log** (the round-33
  entry closed; the "Ship" entry's refresh paragraph for this round),
  **the playbook** (§12's sign-off line) and **the round-33 plan** (its Closed
  section).
- **The mirror.** `main` is refreshed with a merge commit whose tree is this
  `v2` tip's exactly (`git commit-tree <tip>^{tree} -p <main-tip> -p <tip>`),
  pushed as a fast-forward — never a force push — and `release/2026-09-22` is
  moved to the same tip. The mirror commit's own message names both parents.

## 2 · The final version, as shipped

The `v2` tip this round's commit makes, carried by `main` and marked by
`release/2026-09-22`: rounds 33–35 and, from the other sessions, round 34 and round 23's device passes up to the tip's own commit ("Client round 23, device pass 17: the edge trigger — Safari…"). It holds everything signed off on
2026-09-22:

- `/paintings` — rounds 24–31, signed off after round 31 ("paintings page is
  done"); the ship round 32.
- `/map` — rounds 17–22, 27, 29–30, signed off after round 29 ("map page is
  done"); the record round 34.
- **The bottom lane under Android's gesture bar — round 33, signed off after
  its live push ("these edits are done").** On a browser that draws a gesture
  bar over the page (Chrome for Android with gesture navigation), every
  bottom-anchored control sits the gutter above the bar; iOS and every engine
  with no reported bar are byte-identical; `npm run qa:gesture` measures it.
- The chapter pages as of round 23's latest device pass in the tree (another
  session; its device read open).

What the remote held when this round was written (18:22 UTC): `main` at
`8095a82` and `release/2026-09-22` at `130a48c`. The mirror commit's message
names what `main` held at the push. Where another session's push lands
first, the mirror is rebuilt on the new `main` tip — the fast-forward rule
makes the order safe.

## 3 · Verification

Recorded in the commit and the report: `qa:scope` clean against the round-35
manifest; the Actions run for the `v2` push and the one for the `main` push
both green, publishing the same tree; the `main` tip's tree equal to the `v2`
tip's (`git rev-parse main^{tree} v2^{tree}`); `release/2026-09-22` at the
`v2` tip. The site itself is unchanged by this round (docs only) — the live
build is round 33's, verified in its own plan, plus whatever the other
sessions' passes in the tree changed, verified in theirs.

## Revert

The docs: `git reset --hard client-round-35-base` (= `26fff0d`). The mirror:
`docs/v4/DECISIONS.md`, "Ship" — restoring the retired app on `main` is a
force push that publishes it, so only with the workflow disabled or with `v2`
pushed again right after; moving `release/2026-09-22` back is a force push
of the branch.
