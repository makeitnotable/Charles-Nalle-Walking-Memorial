# Client round 44 — round 43 signed off: the runway is the chapter default on phones; shipped

*Wil, 2026-09-22, on round 43 live under the flag: "As far as i can see
the way everything works on the bakery page is how it should work on every
chapter page. As far as I am concerned, this is done push to master and
live site and document everything and the final version that we are
shipping to the client accordingly and to best practices." Base `abb2934`
(= the `v2` tip, round 43), tagged locally `client-round-44-base`; the SHA
is pinned in the manifest. Revert: `git reset --hard client-round-44-base`.*

## 1 · What this round is

One line of code, and the record.

- **The default.** The head script's default branch assigns `"sync"` on
  phones and `"inner"` on iPads: the screen's shorter side under 700 is a
  phone (390–440 on every iPhone; told from the screen, never the viewport,
  which iOS reports as 980 before the viewport meta is parsed — round 23
  pass 14's gate), 744+ is an iPad, whose toolbar never collapses, so the
  runway would cost its one-frame placement for nothing and the still
  document he approved on 9/19 stays. `?scroll=sync|inner|doc` still
  override on one page load.
- **The instrument.** `qa:still`'s default checks move with the default:
  the runway on all five chapters (the mode, the geometry, the reader
  tracking the document, the rail out of the moving box, the colours, the
  menu, the scrub, the mini player portaled after a real play — the control
  brought into view by the mode's own scroller, the runway's being the
  document), the still document on all
  five under `?scroll=inner` (unchanged checks, plus the reader wrapper
  inert), the deep links in both, the iPad by default, the plain document
  off the gate, the three flags, the top inset emulated both ways.
- **The record.** `docs/HANDOVER.md` §10 takes the chapter pages in the
  museum's language (version 1.4); `docs/RUN-STATE.md` the phase;
  `docs/v4/DECISIONS.md` the approval under round 23 and the "Ship" refresh;
  playbook §3 the final rule; `docs/NEXT-SESSION.md` the count; the
  round-43 plan its Closed section.
- **The mirror.** `main` is refreshed with a merge commit whose tree is this
  `v2` tip's exactly (`git commit-tree <tip>^{tree} -p <main-tip> -p <tip>`),
  pushed as a fast-forward — never a force push — and `release/2026-09-22`
  is moved to the same tip. The mirror commit's own message names both
  parents.

## 2 · The final version, as shipped

The `v2` tip this round makes, carried by `main` and marked by
`release/2026-09-22`. It holds everything signed off on 2026-09-22:

- **The chapter pages** — round 23's close (the still document, 9/19–9/20),
  restored by round 38 after the reverted passes 13–17; then the runway,
  tried behind a flag (round 43) and approved on the Bakery: on a phone the
  document scrolls a runway in the section's colour, both bars collapse and
  return, the top bar is solid in that colour, the returning toolbar sits
  over Safari's own fill, the page is placed a frame behind the finger, and
  everything pinned stays out of the moving box; on an iPad the still
  document. Rounds 40–42 (the edge trigger, tried and rejected, removed) are
  in the history only.
- `/paintings` — rounds 24–31 and 36; `/map` — rounds 17–22, 27, 29–30; the
  bottom lane under Android's gesture bar — round 33; all signed off earlier
  the same day (rounds 32, 34, 35, 37).

Not in this tree: round 39 (the map style and token moved to the museum's
Mapbox account), committed on a local branch and held by GitHub's
secret-scanning rule until the token is allowed.

## 3 · Verification (Chromium 141 / Playwright 1.62)

- `npm run build` + `check-css` (6 guards) ✓ · `npm run check` 0 errors, 0 warnings (294 hints) ✓ ·
  `npm run qa:scope` — every changed path in the round-44 manifest ✓.
- **`npm run qa:still`: 296 checks, 0 failed, 20 sessions — the runway on all five chapters (24 checks each: `data-scroll` is `sync`; `<main>` fixed and clipped at 844; the document as tall as the chapter; the reader at 0 at rest and exactly −scrollY after a scroll; the rail after `<main>` in `<body>` at the top edge, 3px, holding while the document scrolls; the cover gone; no residue of the retired passes; the runway brown at rest, cream with the transcript at the top edge, brown again at the top, the theme-color following; the menu one gutter below the rail and retreating; the lockup's scrub against the document; the mini player portaled to `<body>` on the 20px lane after a real play; no page errors), the still document on all five under `?scroll=inner` (25 each: the round-38 checks, plus the reader wrapper inert and the mini player inside `<main>`), `#history` landing within 40px in both modes (the document scrolled there in the runway, at 0 in the still document), the iPad at 768 under the gate taking `inner` with E = 0, the plain document off the gate at 390 and 1440, the three flags, the top inset emulated at 12px (the rail at 12; the menu at 32 in the runway, 20 in the still document).**
- **`npm run qa:snap`: 36 shots (11 routes × 390/768/1440 + the museum rail), 36 ok, 0 drift, 0 failed.** No route may drift: Chromium never takes
  the gate, so the visual gate sees the plain document as before.
- The ship: `v2` pushed first; its deploy run (300, success; https://github.com/makeitnotable/Charles-Nalle-Walking-Memorial/actions/runs/35793299698) published
  the tree, and nothing else was pushed until it finished (the workflow's
  cancel-in-progress concurrency — round 37's lesson). This second push, docs
  only, records that and waits for its own `v2` run the same way; then `main`
  is refreshed once — a merge commit whose tree is this commit's, its parents
  `f1a0e22` (the round-37 mirror) and this commit, pushed as a fast-forward —
  and `release/2026-09-22` is moved to this commit. The mirror's own run
  republishes the same tree.

## 4 · Checklist

- [x] Revert point: `client-round-44-base` = `abb2934` (local tag; the SHA in
      the manifest, and `v2` holds the commit).
- [x] Scope gate: `qa:scope` clean against the round-44 manifest.
- [x] Visual gate: `qa:snap` 36/36, 0 drift.
- [x] The instrument: `qa:still` 296/296.
- [x] Device first: the default is the mode he approved on the Bakery under
      the flag; the other four chapters share the template and the gate.
- [x] Shipped: `v2` deployed; `main` mirrored; `release/2026-09-22` moved.

**Verified here:** everything in §3. **Not verifiable here:** the other four
chapters on his phone (the same template, the same gate; the Bakery was his
read). If one differs, it is a new round.

## Revert

`git reset --hard client-round-44-base` (= `abb2934`), then push `v2` — the
runway goes back behind the flag. To keep the code and only restore the
still document on phones: make the head script's default branch assign
`"inner"` (one word).
