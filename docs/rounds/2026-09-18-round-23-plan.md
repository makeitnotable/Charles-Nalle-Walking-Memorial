# Client round 23 — the chapter pages with fresh eyes

*Wil's four screenshots of 2026-09-18 (11:41–11:43 and 10:29), the five
chapter pages only — Bakery, Commissioner, Mansion, Ferry, Barbershop. Base
`4d13540` (= the `v2` tip and the live site after round 22), tagged locally
`client-round-23-base`. Revert: `git reset --hard client-round-23-base`. His
answers to the diagnosis report's four questions, verbatim: (1) the bottom
toolbar "should have no fill"; (2) "fix everything requested in the screenshot
without breaking the layout or design"; (3) keep the menu's fade as it is;
(4) tablets only in a browser with collapsing bars — "Correct". And the bar:
"These pages should all look and perform stunningly across every device and
screen size, with the content being as immersive as possible."*

## 1 · The area behind the top bar follows the section

**His words.** "Set the area behind/around the browser's top UI (address bar /
toolbar / safe-area inset region) to a solid fill that matches the background
color of the section currently visible at the top of the viewport. The color
should update as the user scrolls between sections. Example: if the hero
section is currently visible, use the hero section's brown background color."
Three shots, all with the address bar minimized: the painting through the pill
over the hero; the heading ghosting through over the quote; the cream showing
through over the story with a dark band at the very top.

**Diagnosis.** Every chapter route handed Safari one static colour, the hero
brown `#100a06`, for the whole page (round 8 phase E, his instruction then).
Safari uses that colour only as a tint. Once the bar minimizes the layout
viewport starts below the pill (playbook §1: 645 at rest, 753 minimized, the
pair 199), and the region above it — status bar and pill — is glass over what
the document holds ABOVE the viewport's top edge: what has just scrolled past,
under a short gradient of `<body>`'s colour. That is all three shots at once.
The dark band over the cream is the brown gradient. Round 18's last attempt
(hypothesis 2: opaque strips at the edges, on screen, under `<main>`) did not
take — his shots are the result — so it is retired.

**What ships.**

- `<body>` follows the section at the top edge on the chapter routes — the
  round-21 rule from `/map`, gated on `data-top-bar="section"` as well as
  `data-root-fill="map"`. The resolver (`sectionGroundAt`) now counts `<main>`
  (the page ground since round 14) and `.ground-cream` blocks as grounds, and
  falls back to `<main>` before `<body>`: the story transcript and the history
  section paint their cream on a `<div>` inside the `<section>`, and the old
  walk fell past it to `<body>` — so over the cream the answer was the hero's
  brown (a latent bug in the Chrome-for-Android meta as well; fixed for both).
  The static `#100a06` rule stays as the at-rest and no-JS value; at scroll 0
  the sampler writes the same value, so nothing moves at rest.
- The **chapter cover**, `.edge-cover`: a 320px absolute box in the document
  (a child of `<body>`, in the scrolling layer, never fixed), moved on every
  scroll event so its BOTTOM edge rides on the viewport's top edge
  (`translateY = scrollY − 320`), coloured by the sampler with the same
  section colour. It covers only what has scrolled past — the reader sees it
  through the bar and nowhere else — which is the one region in-flow paint
  reaches and fixed paint does not (A2's clip; the map's runway). Its bottom
  16px are feathered by a mask: scrolling is asynchronous on iOS, so a fast
  upward flick can leave the cover a frame behind, and the feather turns a
  hard band into a faint haze under the pill. `display: none` unless
  `-webkit-touch-callout` is supported (the iOS gate the playbook records), so
  desktop, Android and the visual gate never see it and no listener is
  attached there. Z 800: over everything in `<main>`, under the walk rail, the
  mini-player and the menu. Device flags, inline and one page load:
  `?cover=off` (a before/after), `?feather=<px>` (0 = a hard edge).
- The chapter strips (round 18) are gone: the chapter routes no longer force
  any opaque extension at either edge. Nothing is painted behind the bottom
  toolbar — his answer to question 1.

**Residue, reported before shipping.** Safari has one tint for both bars, from
`<body>`. So when the bottom toolbar is expanded it carries the top section's
colour as a cast over whatever is under it — invisible while top and bottom
are in the same section, a faint mismatch across a section boundary. No
page-side lever separates the two bars; the page paints nothing there.

**What only the phone can show.** Whether Safari composites the cover through
the pill the way it composites the runway (the same in-flow mechanism, so it
should); how far a fast flick leaves the cover behind, and whether 16px of
feather hides it; how Safari's own tint animation (~300 ms) reads against the
cover's instant switch at a section boundary.

## 2 · The corner menu holds its place

**His words.** "The menu animates by sliding from the right to the left and
from up to down slightly when the user scrolls. (It's like its position is
getting pushed by the scroll bar.) On scroll up the menu should fade in; on
scroll down the menu should fade out. Do not use a slide-in/translate-from-
right animation for this menu."

**Diagnosis.** It was never animated. `--ui-inset` is one lane for every
corner and takes the deepest of the four safe areas; `env(safe-area-inset-
bottom)` is 0 with the bars expanded and 34px once they collapse (playbook
§1). So the top-right burger jumped from 20px to 34px on both axes — 14px down
and 14px left — on every scroll down and back on every scroll up, in the same
moment its retreat fade ran. The playbook listed this shift as open, by his
decision; this is the decision.

**What ships.** The top-right menu reads its own `--menu-inset`: the gutter,
the top inset and the right inset — its own two edges — and nothing at the
bottom edge can move it. The fade is unchanged (opacity plus the small settle
it already has; his answer to question 3). Every page that shows the menu
places it top-right (the map moved there in round 1), so this is the
site-wide behaviour he asked for. No other corner control changes: the
mini-player and the interlude credit still ride `--ui-inset` (RUN-STATE, open).

## 3 · The hero lockup sits on the gutter

**His words.** "On mobile (and tablet if the bottom toolbar is present), move
the hero section's text on all five chapter pages (excluding only part 2 of
chapter 2) down so that the vertical spacing between the bottom of the chapter
title and the top of the bottom toolbar feels optically the same as the
horizontal spacing between the left of the hero section text elements and the
left edge of the screen."

**Diagnosis.** The hero is one small viewport tall, so at rest its bottom edge
is the toolbar's top. The lockup's inset was `max(5svh, 32px)` — 32px on his
phone — plus the title line box's air under the baseline (Libre Caslon
Display at `--lh-display` 0.95: `(0.95 − 0.970 + 0.266) / 2` = 0.123em, 6px at
the phone size): the eye read ~38px against a 20px gutter.

**What ships.** On phones (below 768) the shell's inset is `var(--gutter)` and
the title's negative bottom margin takes the line-box slack out — ink to ink,
not box to box: on the left the eye measures the gutter plus the capital's
side bearing (~0.05em), so the margin removes 0.123 − 0.05 = 0.073em and the
last line's ink sits the same distance above the edge as the first letter's
ink sits from the left. On tablets (768–1023) the same, but only in a browser
with collapsing bars: `100lvh − 100svh` is the bars' height there and 0 on an
iPad and every desktop, and a trig cast turns that length into a 0/1 switch
(`clamp(0, tan(atan2(100lvh − 100svh, 1px)), 1)`), behind an `@supports` gate
so an older browser keeps the old inset rather than losing it. Desktop (1024
and up) and Commissioner's Part-2 hero (`#hero-2`) are untouched.

## Verification (measured 2026-09-18, Chromium 141 / Playwright 1.62)

- `npm run build` + `check-css` ✓ · `npm run check` 0 errors, 0 warnings ·
  `npm run qa:scope` — every changed path in the manifest ✓.
- **The round-23 probe** (a scratch Playwright script, not committed) at
  390×844, 390×645 and 360×780 on all five chapters: the last line's ink sits
  22–23px above the hero's bottom edge, the first letter's ink 22–24px from
  the left edge — the same number both ways on every chapter; `#hero-2`'s
  inset unchanged at `max(5svh, 32px)`. 768×1024: unchanged (60px below the
  ink, as the base build) unless the bars are stood in (`--hero-bars: 1`),
  then 44px against 43px on the left. 1440: unchanged.
- Menu: at rest top/right 20px; with `--ui-inset` stood at 34px (the collapsed
  state) the menu stays at 20px while the token reads 34px, on `/bakery`,
  `/people` and `/map`; with `--gutter` stood at 47px (a side inset) the menu
  follows to 47px.
- The cover, gate stood in at 390×844 on `/bakery` and `/commissioners-office`
  at 21 scroll depths (hero, its tail, the quote, the transcript twice, the
  interlude, history, the moral, the Part-2 hero, onward, the end): at every
  depth the cover's colour, `<body>`'s colour and the `theme-color` meta all
  equal the ground box found independently at the top edge (`#100a06` on both
  heroes, `#1d1411` on the page ground, `#f6f3ee` on the transcript, the
  interlude and history), the cover's bottom edge is at 0.0 (never inside the
  viewport), its height 320. Off the gate (Chromium) it is `display: none`
  with no inline transform — no listener attached — while `<body>` still
  tracks. `?cover=off` → `display: none`; `?feather=0` → `0px` on `<html>`.
- `npm run qa:snap:update`: the five chapter routes' 390 shots refreshed by
  design (the lockup moved); every 768 and 1440 shot and every other route
  unchanged (see the commit message for the table's totals).

## Device pass — what to look at, and what a FAIL looks like

| Where | Expected | FAIL looks like |
|---|---|---|
| A chapter, scrolled a little (bar minimized) | The status bar and the pill sit on solid hero brown; no painting through the glass. | The painting still shows behind the pill. |
| Scrolled into the quote | Solid page brown behind the pill. | The heading ghosts through. |
| Scrolled into the story | Solid cream behind the pill, no dark band at the very top. | A dark band at the top, or the text ghosting. |
| A fast flick UP | Nothing, or at most a momentary band at the very top while the flick is fast. | A band that lingers or flashes on every flick — try `?feather=16` or `24` to soften the edge (the default is hard since device pass 1). |
| Scroll up so the bottom toolbar returns | The page continues behind it, no fill; near a section boundary a faint cast of the top section's colour is the residue above. | A solid band under the toolbar. |
| The menu, scrolling down then up | Fades out and in, in place. | It moves diagonally. |
| The hero at rest, toolbar visible | The title's last line sits as far above the toolbar as its first letter sits from the left edge (~22px). | Still ~40px. |
| Commissioner's Part-2 hero | Unchanged. | Moved. |
| iPad, desktop, Android | Unchanged. | Anything moved. |

`?cover=off` shows any chapter without the cover for a before/after.

## Device pass 1 (2026-09-18, 15:13) — and the second push

Wil's screenshot: the Bakery story section with the address bar minimized.
The region behind the status bar and the pill reads the transcript's cream —
**the cover is composited through the pill**, the first device confirmation
that in-flow paint above the viewport's top edge reaches the bar region on a
chapter page, as the runway does below it on `/map`. "Almost perfect! Just
should be a solid fill not a gradient."

**The gradient was the feather.** Through the cover's feathered bottom 16px
the section just above the viewport's top edge ghosted in — at that scroll
position the dark player ground sits a few pixels above the cream transcript's
top edge — and a soft edge over a two-colour boundary reads as a gradient,
which is exactly what it is. The default is now `0px`, a hard edge; the mask
and the `?feather=<px>` flag stay so the edge can be softened on the device if
a fast flick ever shows the cover trailing the scroll. Nothing else changed.
The visual gate cannot see the cover (display:none in Chromium), so no route
may drift; `qa:snap` in compare mode is the check.

Still to look at on the phone, unchanged from the table above: a fast flick
up with the hard edge; the bottom toolbar's cast at a section boundary; the
menu holding still; the hero inset at rest.

## Device pass 2 (2026-09-18, 17:41) — and the third push

Wil, two screenshots (the hero and the history section, bar minimized): "The
background fill behind/around the browser address bar changes height as you
scroll (it looks like it grows/shrinks). It must be a solid, stable fill that
never changes height or size while scrolling." And: "There's a thin gap/slit
above the chapter page indicators (the orange progress bars) where underlying
text shows through. This area must be fully covered by the same solid fill."
In both shots the cover's bottom edge sits 3–6px above the rail, a different
amount in each.

**Diagnosis.** The tracker set the cover's transform from `scrollY` on each
scroll event and never re-read it. The position in the last scroll event is
not where iOS finally settles — the compositor finishes the scroll after the
last event the main thread sees — so at rest the cover was short by the
remainder, a different remainder after every scroll: the "growing and
shrinking". And the cover's edge was aimed at the viewport's top with the
rail's 3px line sitting on it, so any shortfall opened a slit of content
between the two.

**What ships.**

- The cover's bottom edge is defined by the rail: it sits on the rail's bottom
  edge, so its last 3px lie behind the rail's line (z 800 under 900). A
  shortfall of up to 3px cannot show, and the fill ends exactly where the rail
  is, with nothing below it.
- Where the browser supports scroll-driven animations (`animation-timeline:
  scroll()`; Safari 26), the compositor moves the cover: a linear animation on
  the root scroll timeline whose range is an absolute 0–100000px of scroll
  offset, so `translateY = offset + rail − 320` at every frame, from the true
  offset, with no main-thread lag and no stale value. The script self-checks
  it on the first scroll events past 60px — the cover's rendered bottom must
  sit on the rail's, or a browser mapping the range differently would show a
  slab sweeping the page — and switches the animation off if it fails.
- The scroll-event tracker stays as the fallback, and behind `?cover=js` on
  the device: it anchors to the rail from the layout viewport's offset
  (`visualViewport.pageTop − offsetTop`, so a visual/layout split cannot open
  a slit) and keeps re-reading on requestAnimationFrame for 600 ms after the
  position last changed, so the settled state is the one painted.

**The hero inset — verified, and why it can look unchanged.** The deployed
build carries the round's rules: at 390 wide the lockup's inset is the gutter
and the title's last line sits 22–23px above the hero's bottom edge, which at
rest is the toolbar's top (the probe's numbers above, on the build Actions
runs 248 and 249 deployed). The lockup also RISES as the page scrolls — the
v4 scrub moves it 60px up over the first 55% of the hero — so in any scrolled
view, including the device-pass-2 hero shot, the gap below the title is 22px
plus up to 60px of that rise, and the toolbar has collapsed by then. The
number to check is at rest: page top, bars expanded, toolbar visible.

**One hazard found on the way, and closed.** The probe's own still-sheet
(the snap gate's: every `animation-duration` forced to 0s) parked the
scroll-driven animation on its END keyframe, 100000px down the page — a
scroll timeline with a 0s duration is instantly finished — and the
self-check duly switched it off. The site carries the same idiom for
`prefers-reduced-motion: reduce` (`animation-duration: 0.01ms !important`,
global.css), so under Reduce Motion the cover would have done the same on a
phone. The animation now applies only under `prefers-reduced-motion:
no-preference`; under `reduce` the rail-anchored tracker drives the cover —
not because the cover is motion (nothing the reader sees moves) but because
that is the only way to be certain of it. And the self-check now runs at rest
too: a failure is visible at any offset, a pass only from 60px.

**Verification.** build + check-css; astro check 0 errors 0 warnings;
qa:scope; the round-23 probe on three paths — the animation, `?cover=js`, and
a `prefers-reduced-motion: reduce` context — two chapters, 21 depths each:
the cover's rendered bottom equals the rail's bottom at every depth, the
colours agree, the document's height is unchanged, and the path in charge is
the intended one (the animation with no inline transform; the tracker with
one); a frame-by-frame sample through a scroll that mounts the transcript
islands and grows the document: the error is 0 on every frame and the
self-check never fires; a wheel scroll sampled every frame on the animation
path: the cover's bottom never leaves the rail's; qa:snap compare 36/36, 0
drift (the cover is display:none in Chromium).

**Only the phone can show** whether Safari runs the scroll timeline on its
compositor (then the fill is exact at every frame) or on the main thread
(then it is exact at rest and a frame behind in motion, the same as
`?cover=js`).

| Where | Expected | FAIL looks like |
|---|---|---|
| Any chapter, scrolled, at rest | The fill ends on the rail line every time, no slit, the same height after every scroll. | A slit above the rail, or the fill's edge in a different place after a scroll. |
| A fast flick, either direction | The fill's edge stays on the rail while moving. | It trails and catches up — say so, and compare `?cover=js`. |
| The hero at rest, toolbar visible | The title's last line ~22px above the toolbar, the same as its first letter from the left edge. | Still ~40px — then say which chapter and send the at-rest shot. |

## Device pass 3 (2026-09-18, evening) — and the fourth push

Wil: "Closer but there is a bunch of weird flicker and distortion that occurs
when scrolling up and down at different speeds. Also the solid extends
slightly beyond the progress indicator. Color change works good but the hero
section fill on the address bar is too dark; it should be the same as the
hero section brown."

**The flicker at speed.** Two candidates, and this push closes both. (1) The
self-check ran on every scroll event, in motion. On a phone the main thread's
reading of the animation can sit a frame behind the compositor, so a fast
scroll could read as a failure, switch the animation off for good, and hand
the cover to the scroll-event tracker — whose lag then shows on every scroll,
speed-dependent, exactly as described. The check now runs at rest only,
300 ms after the last scroll or resize event, when the two views agree. (2)
The cover carried a mask — a degenerate two-stop gradient once the feather
was 0 — on a moving composited layer; it is gone unless `?feather=<px>` asks
for a soft edge. What the phone still decides is whether Safari runs the
scroll timeline on its compositor at all; the `?cover=debug` readout below
says which path is driving.

**The fill beyond the indicator.** The cover ended 3px behind the rail's line,
and through the line's 2px gaps and its translucent "ahead" segments that read
as fill reaching the line's bottom. It now ends 1px inside the top of the
line: the indicators sit below the fill, touching it, and a sub-pixel edge
still lands under the line rather than above it. Both paths and the
self-check use the same edge.

**The hero's brown.** The heroes' own `background-color` was
`--color-neutral-2` #100a06, a near-black nobody sees — the painting covers
it — and the round-8 fallback. The brown he means is the page ground #1d1411,
which the scrim under the lockup already ends in. Both heroes now carry
`bg-primary-2`, so the resolver, the cover, `<body>` at rest, the
Chrome-for-Android meta and the page ground agree on #1d1411. Under the
poster at rest, so no shot moves.

**`?cover=debug`.** A small readout fixed at the bottom left of any chapter:
which path drives the cover (compositor, tracker, or tracker after a failed
check), the check count, `scrollY`, `visualViewport.pageTop` and `offsetTop`,
`innerHeight`, the rail's top and the cover's rendered bottom, and the
colour. One screenshot of it, scrolled and at rest with the bar minimized,
answers what this container cannot: the visual/layout split on his phone, and
whether the compositor path is in charge.

**Verification.** build + check-css; astro check; qa:scope; the probe on the
three paths, two chapters, 21 depths each: the cover's bottom sits 1px inside
the rail's top at every depth, both heroes resolve to #1d1411, colours agree,
the document height is unchanged; the wheel-scroll sample: 0px; the debug
readout renders and names the compositor path; no mask by default, a mask
under `?feather=`; qa:snap compare 36/36.

## Device pass 4 (2026-09-18, 20:14) — and the fifth push

Wil, a crop of the expanded address bar mid-scroll over the archival photo:
"This is what happens when scrolling. It also looks buggy / jittery and not
smooth when the content scrolls through the area behind and around the
address bar. Also the fill still changes size." In the crop the rail sits at
the layout viewport's top (108 CSS px, under the expanded bar) and the
cover's edge ~30px above it, with the photo showing between and blurred
under the pill.

**What the crop proves.** A 30px gap at speed is a frame's worth of scroll.
The cover was being placed from the main thread on his phone — either the
scroll timeline is not accelerated in his Safari, or the script was
driving — and nothing positioned from the main thread can be smooth in the
bar region: the compositor draws that region from the true scroll offset
every frame, and a main-thread box is always one frame behind, by an amount
that varies with speed. That IS the jitter, and it IS the fill changing
size. The only in-flow thing the scrolling thread itself places is
`position: sticky`.

**What ships.** The cover is a sticky box, first in `<body>`'s flow: 320px
tall with a −320px bottom margin (so `<main>` still starts at 0 and the
document is not one pixel taller), drawn 319px higher than it is laid out,
with `top: 0`. From the first pixel of scroll the scrolling thread holds its
layout box on the viewport's top edge, so the painted box always spans the
region above that edge — no script, no timeline, no lag, and its last pixel
inside the rail's line. The script only colours it. The tracked variants
stay behind flags: `?cover=track` (pass 3's scroll timeline, self-checked)
and `?cover=js` (the script); `?cover=off` and `?cover=debug` as before, the
readout now naming `sticky`.

**What only the phone can show.** Whether WebKit paints a stuck box's pixels
in the bar region the way it paints the scrolled runway's. Round 8's A2 saw
a pinned box's *overflow* clipped at the viewport, but that box also carried
`overflow: clip`, which Safari applies without the clip-margin Chrome
honours, so the measurement does not settle it; a sticky box painting its
own bounds is untested. If it does not paint there, the bar shows content
again (the pass-0 look, tint tracking still on) and `?cover=track` restores
pass 3.

**Verification.** build + check-css; astro check; qa:scope; the probe on
four modes (sticky, `?cover=track`, `?cover=js`, reduced motion), two
chapters, 21 depths each: the cover's bottom sits 1px inside the rail's top,
`<main>` at 0, the heroes #1d1411, colours agree; the sticky box adds no
height to the document (with and without the cover, byte-equal heights, main
and hero at 0); a wheel scroll sampled every frame: 0px; qa:snap compare
36/36 (display:none in Chromium).

## Device pass 5 (2026-09-18, 20:35) — and the sixth push

Wil, a crop of the minimized bar over the quote section with the heading
ghosting through: "Now it's transparent and there's no solid fill behind or
around the top address bar at any point scrolling up or down. It's supposed
to be a solid fill around and behind the address bar when scrolling up and
down; the colour of the fill should change based on the section."

**Measured: a stuck box does not paint in the bar region.** `position:
sticky` is clipped at the viewport exactly like `position: fixed`. That
closes the list: the region behind the bars shows scrolled page paint and
Safari's own tint, nothing else. Round 8's tests 4 and 5 cover the one other
lever — an element Safari reads for its fill has to be a VISIBLE opaque
strip, and it is read as a tint source (the bar took the strip's colour at
rest; nothing showed that paint surviving the minimized state's glass). So a
solid fill can only be scrolled page paint, positioned by the page, and on
his phone that is a frame behind at speed whether a script or the timeline
drives it (pass 4).

**What ships.** The scroll-event tracker is the default again, rail-anchored
and settling at rest as in pass 2, and its bottom edge is now FEATHERED IN
PROPORTION TO SCROLL SPEED: hard and exact at rest (the state pass 1 got
right for colour and pass 2 for position), soft only while the page moves —
feather = speed × 40 ms, capped at 64px, 0 below a slow drift — so the frame
of lag reads as a soft edge under the pill rather than a slit of content or
a jumping line. The mask exists only while the feather is non-zero
(`is-soft`). Pass 1's fixed 16px feather was the version he called "almost
perfect"; its gradient at rest is what the speed-proportional one removes.
The timeline stays behind `?cover=track` with its at-rest self-check;
`?debug=1` is its own flag now and reports, besides the driving path and the
geometry, whether the browser supports `animation-timeline: scroll()`, how
it parsed `animation-range-end`, and its Safari version.

**What only the phone can show.** How the soft edge reads at his scroll
speeds, and — from `?cover=track&debug=1` — whether his Safari supports the
timeline, honours a length range, and runs it on the compositor. If all
three, the timeline becomes the default and the feather is no longer needed
in motion.

**Verification.** build + check-css; astro check; qa:scope; the probe on
four modes (the tracker, `?cover=track`, `?cover=js`, reduced motion), two
chapters, 21 depths each: at rest the cover's bottom sits 1px inside the
rail's top, the feather is 0 and the mask absent, `<main>` at 0, the heroes
#1d1411, colours agree; a wheel scroll sampled every frame: the feather
rises in motion and settles to 0 (hard) at rest; the debug readouts render
for both paths; qa:snap compare 36/36.

## Device pass 6 (2026-09-18, 22:42) — and the seventh push

Wil, three crops, both scroll directions, both bar states: the content behind
the bar showing in a 10–40px band above the rail whenever the page moved, the
pass-5 feather only blurring its top. "Take a look at everything with fresh
eyes. The problem has not yet been solved. There should never be a moment
where the area behind or around the top address bar is see-through or
transparent. It should always be only ever a solid fill. The solid fill can
change color instantaneously … no gradients no transitions just one color
that fills the area behind and around the address bar and changes based on
the section color."

**Fresh eyes.** Six passes have measured the whole space: behind the bar only
scrolled page paint shows; nothing pinned reaches it; Safari's tint is a
gradient; and page paint is placed by the page, a frame late while the page
moves. A cover aimed at the viewport's top edge therefore exposes its lag IN
the bar region, where nothing can hide it — that is every crop since pass 2.
The lag cannot be removed. It can be moved somewhere a fixed box CAN hide it:
inside the page.

**What ships — the visor.** The walk rail becomes a fixed, opaque band of the
section's colour, `--edge-visor` = 32px tall, its 3px indicator line at the
bottom. The cover's bottom edge is aimed at the visor's MIDDLE, not the
viewport's edge. A frame of lag in either direction now moves that edge up or
down inside the visor, hidden by it; above the visor the bar region is solid
cover, below it the page, and both stay solid at every speed. The script also
leads the scroll by its measured speed × about a frame and a half (the frame
interval is measured too), so the edge stays near the middle at speed and the
visor's half-height absorbs the residual; at rest the edge settles in the
middle and nothing depends on where exactly, so the at-rest slit of pass 2 is
gone by construction. The sampler writes the section's colour to the cover
and the visor alike, instantly, no transition — as he asked. The feather is
gone. The cost is the band itself: the solid fill ends 32px below the
viewport's top edge rather than at it, with the indicators at its foot — which
is the design he described from the start, a solid fill that ends at the
progress indicators, only a little deeper. `?visor=<px>` tries another height
on the phone (24 tighter, 48 safer at extreme flicks); `?cover=off` removes
cover and visor; `?cover=track` aims the timeline at the same middle.

**What only the phone can show.** Whether 32px absorbs the residual at his
flick speeds (a 60Hz phone at 1000px/s leaves ~16px of jitter after the
lead), and how the deeper band reads to him. The alternative that removes
the frame of lag altogether — scrolling the chapter inside an inner
container so the document itself never moves and the bar region shows only
the canvas — would most likely stop Safari's bars from collapsing at all,
which is his decision to make, not this round's.

**Verification.** build + check-css; astro check; qa:scope; the probe on
four modes, two chapters, 21 depths each, with the visor rule stood in
beside the cover's: at rest the cover's bottom sits at the visor's middle,
the visor is 32px in the section's colour with its 3px line ending at its
foot, `<main>` at 0, the heroes #1d1411, colours agree, the document height
unchanged; a continuous scroll (24px a frame, a decel, a reversal) sampled
every frame from the main thread: while moving the edge leads the middle in
the direction of travel by up to 37px (the lead, vel × ~1.5 frames, which
Chromium's lag-free main-thread scroll shows in full), never opposes the
motion, and is exactly on the middle at rest — the lag it is sized to
cancel exists only on the phone, so whether 32px absorbs the residual is a
device question; `?visor=24` and `?cover=off` behave; qa:snap compare 36/36
(visor and cover are display:none off iOS).

## Device pass 7 (2026-09-19, 06:15) — and the eighth push

Wil, a crop of the expanded bar over the Bakery painting with the visor in
place: "Even better but now it's too tall. And there is a little flicker
(must be a small slit/gap?) that flashes the content behind it when
scrolling both fast and slow."

**Two asks that pull against each other.** The band's height WAS the
tolerance, and it was spent twice: half above the middle for lag, half below
for the lead. Shrinking the band halves the room for the one failure he can
see. So the geometry spends the whole band on that failure. The cover now
aims at the TOP OF THE VISOR'S LINE — its foot — not its middle. Everything
above the line is room for the edge to lag upward before the bar region shows
content; an edge that overshoots downward only runs behind the line (the rail
is z-index 900, above the cover's 800, and the band's colour is already
behind the line's gaps) and then over the page's first rows in the section's
own colour — never see-through.

**The lead, re-read.** Pass 6's lead was symmetric: speed × 1.5 frames in the
direction of travel. Scrolling DOWN that put the steady state half a frame
below the aim, hidden. Scrolling UP it put the steady state half a frame
ABOVE the aim — toward exposure — and at a moderate 1–2px/ms that half-frame
is 8–16px, the whole of pass 6's 16px of room: the slit he saw "both fast and
slow" on the way up. The lead is now one frame of motion (the compositor's
head start on anything the main thread places) plus half a frame DOWNWARD
whichever way the page moves, so the steady state sits half a frame below
the aim in both directions. Its smoothing also carried the old direction
across a reversal for a frame; a reversal now takes the new sign at once.
The rail's rect is measured at init and on resize instead of in every scroll
event (a forced layout on a busy page).

**What ships.** `--edge-visor` defaults to 16px: 13px of room above the line,
against pass 6's 16 from a 32px band — half the height for nearly the same
room, and no room spent on the side he cannot see. `?visor=<px>` still tunes
it (`?visor=3` is the bare line of pass 3, no room at all; `?visor=24` gives
21px of room); `?cover=track` aims the timeline at the same point; `?debug=1`
now prints the aim.

**What no lead can absorb.** A dropped main-thread frame: at 1px/ms one costs
~8px of the room, two ~25px. Only the compositor path has no such frame, and
only if his Safari accelerates the scroll timeline — `?cover=track` beside
the default, and the `?cover=track&debug=1` readout, are the two things his
eyes must answer. If it does, the band can shrink to the line itself. If it
does not, the honest choices are a band sized to his flick speeds or the
inner scroll container that stops the bars collapsing — his call either way.

**Verification.** build + check-css; astro check; qa:scope; the probe on
four modes, two chapters, 21 depths each, with the visor rule stood in
beside the cover's: at rest the cover's bottom sits on the top of the
visor's line, the visor is 16px in the section's colour with its 3px line
ending at its foot, `<main>` at 0, the heroes #1d1411, colours agree, the
document height unchanged; a continuous scroll sampled every frame from the
main thread (Chromium's lag-free scrollTo shows the raw lead): scrolling
down the edge sits 6–40px BELOW the aim and never above it, scrolling up
2–10px above it and never below, exactly on the aim at rest; `?visor=24`
aims 21px in; `?cover=off` behaves; qa:snap compare 36/36 (visor and cover
are display:none off iOS).

## Revert

`git reset --hard client-round-23-base` (= `4d13540`), then push `v2`.
