# The Award Audit Prompt (paste verbatim into a new Opus 5 session)

Work in `/Users/thebayniac/Documents/(A) Documents/(A) WBM Enterprises/(B) Notable/(B) Clients/Charles Nalle/cnwm-v2` (branch `v2`). Read `docs/PLAN.md` (context + compaction protocol — disk is truth, update `docs/RUN-STATE.md` every sub-step, commit and push relentlessly) and `docs/CONTENT-STATUS.md` before touching anything.

**The verdict you start from:** I built this to be award-winning and it is not. There are UX issues, UI issues, design issues, motion and animation issues, and overly repetitive content — a large number of small failures whose sum is a site that looks *good* and feels *unfinished*. Your job: find every one of them, tell me why the site fails the award bar, then fix all of it. Do not defend the current build — you did not make it, and I am telling you it fails. The bar is absolute: **meets or exceeds the five inspiration sites, capable of actually winning.**

**Confirmed defects I can see immediately (these are facts, not hypotheses — verify, then hunt each one's ENTIRE class across every page and breakpoint; they are automatic P0s and the first things fixed):**
1. **Header/display type is way too big.** The display type system (`t-display` / `--fit-chars` fit-text) is producing absurd sizes somewhere in the matrix. Audit every heading at every breakpoint against the approved ×1.25/×1.5 ladder; headers must feel commanding, never clownish; measure rendered px and cap the fit system.
2. **On the chapter pages, the same painting appears three times.** This is the one confirmed content duplication: a single image doing triple duty on one chapter page (likely the `horizontal` painting serving hero + a later section + a nav/footer slot). Fix the chapter template so every image slot draws a *distinct* asset from the 95MB media library — each chapter has sketches, a historical photo, moral art, square/vertical crops, and (ch2/4/5) part-2 or narrative paintings available; where a chapter truly lacks variety for a slot, change that slot's layout rather than repeat the image. Then sweep the remaining pages for the same class, since a template bug rarely lives alone.
3. **The menu is broken: buttons overlap other buttons.** Audit ALL fixed/floating UI simultaneously at every breakpoint and scroll depth — menu button, audio mini-player, map carousel, back button, geolocate/hint toasts, dialog close. Map every collision, define one stacking/placement system (reserved corners/lanes per element), and prove no two floating elements ever overlap in any state combination (menu open + audio playing + map focused is a state that must be screenshot-tested).
4. **Layout/positioning is plain sloppy in multiple places** — content misaligned, inconsistent offsets, elements sitting at eyeballed positions. This is a class, not an instance: run an alignment pass per page (grid lines overlaid on screenshots if needed) and normalize every offset to the spacing scale.

## Stage 1 — Audit (no fixes yet; evidence only)

1. Build + serve locally, and capture the LIVE site too (`makeitnotable.github.io/Charles-Nalle-Walking-Memorial/`). Using Playwright (installed): every route × **390×844, 768×1024, 1024×768, 1440×900** + landscape phone, at top AND at 4–6 scroll depths per page, plus interaction states (menu open, audio playing, map card focused, dialog open, press-reveal mid-hold, curtain mid-transition, hover and focus states). Screenshots are the only admissible evidence — never reason from the code's intent.
2. Judge like an **Awwwards juror**, scoring every page × breakpoint on their real axes: **Design 40% · Usability 30% · Creativity 20% · Content 10%**, each 1–10. SOTD reality: ~8.5+; anything under 7 on any axis is a named failure. For every score below 9, write down the *specific visible reasons* — screenshot-referenced, one finding per reason.
3. Sweep for the death-by-small-cuts layer, minimum **60 distinct findings** across: spacing/alignment inconsistencies, type rag/widows/orphans, inconsistent casing and label styles, easing/duration mismatches, missing hover/focus/active states, abrupt or missing enter animations, image crops and aspect breaks, layout shift, repeated/near-duplicate copy (audit hypothesis, not yet user-confirmed: the portal-vs-historical text duplication and same-scaffold-every-chapter sameness from the original content port — verify whether these survive in the current build and fix only what the pixels confirm), identical CTAs stacked near each other, dead ends, orphaned UI, loading jank, console noise. Fewer than 60 findings means you weren't looking — go back.
4. Root-cause synthesis: beyond the itemized list, answer in plain language *"why does this fail to feel award-winning?"* — the 3–5 structural reasons (e.g., uniform rhythm with no crescendo moments, motion that decorates instead of narrates, content that repeats instead of deepens, pages that all peak at the same volume). Compare directly against the five inspiration sites in `docs/INSPIRATION.md`.
5. Write it all to `docs/v5/AUDIT.md`: scores table → root causes → findings ranked P0 (breaks the award case) / P1 (visibly unpolished) / P2 (detail), each with screenshot path, breakpoint, and the exact fix. Commit.

## Stage 2 — The fix plan

Convert the audit into `docs/v5/FIX-PLAN.md`: every finding mapped to a concrete change, in this execution order — **the four confirmed defect classes first** (type scale, duplicate imagery, floating-UI collisions, alignment normalization), then remaining structural fixes (layout rhythm, content dedup, motion system), then the small-cuts sweep. Content edits: deduplicate and tighten ruthlessly but **never alter Kathy Sheehan's approved facts** — cut repetition, vary chapter rhythm using each chapter's unique assets (ch2's two scenes, ch4/5's narrative paintings), sharpen microcopy. Commit.

## Stage 3 — Execute everything

Work through FIX-PLAN in order. Every fix: implement → screenshot the same view again → check it off in RUN-STATE with before/after paths. No fix is "done" from code — only from pixels. Push at least every 3 commits; CI deploys automatically.

## Stage 4 — The gate (do not stop before this passes)

Spawn a **fresh adversarial juror subagent** (no builder context): current screenshots + live URL only, same Awwwards axes, instructed to look for reasons to score LOW. Requirements to finish: **every page ≥8 on every axis at every breakpoint, zero P0/P1 findings, and two consecutive juror passes that surface nothing new.** Each failed pass → fix → re-run. Then re-verify the LIVE deploy end-to-end, update `docs/v5/REVIEW-GUIDE.md` (before/after pairs, scores, what changed and why it now clears the bar), update memory, push.

Ground rules: fully autonomous, no check-ins; queue anything only-I-can-do in the review guide. Playwright pixels over markup greps, always. Compaction protocol from `docs/PLAN.md` applies from minute one. The morning test is unchanged: familiar to the stakeholders who approved the design — and undeniably, specifically, award-caliber.
